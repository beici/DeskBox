<?xml version="1.0" encoding="utf-8"?>
<views:WidgetWindowBase
    x:Class="DeskBox.Views.QuickCaptureWidgetWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:DeskBox.Controls"
    xmlns:viewModels="using:DeskBox.ViewModels"
    xmlns:views="using:DeskBox.Views"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="DeskBox Quick Capture">

    <Grid
        x:Name="RootGrid"
        Background="Transparent"
        IsTabStop="True"
        KeyDown="RootGrid_KeyDown"
        AllowDrop="True"
        DragOver="RootGrid_DragOver"
        Drop="RootGrid_Drop">
        <Grid.Resources>
            <!-- Widget-local semantic foreground resources. -->
            <SolidColorBrush x:Key="TextFillColorPrimaryBrush" Color="#FFF5F5F5" />
            <SolidColorBrush x:Key="TextFillColorSecondaryBrush" Color="#E8FFFFFF" />
            <SolidColorBrush x:Key="TextFillColorTertiaryBrush" Color="#CCFFFFFF" />
            <SolidColorBrush x:Key="TextFillColorDisabledBrush" Color="#8FFFFFFF" />
            <SolidColorBrush x:Key="ControlStrongFillColorDefaultBrush" Color="#FFF5F5F5" />
            <SolidColorBrush x:Key="ControlStrongFillColorDisabledBrush" Color="#8FFFFFFF" />
            <SolidColorBrush x:Key="ControlStrongStrokeColorDefaultBrush" Color="#E8FFFFFF" />
            <SolidColorBrush x:Key="ControlStrongStrokeColorDisabledBrush" Color="#8FFFFFFF" />
            <SolidColorBrush x:Key="DividerStrokeColorDefaultBrush" Color="#66FFFFFF" />
            <SolidColorBrush x:Key="WidgetDragHandleBrush" Color="#CCFFFFFF" />
            <SolidColorBrush x:Key="ButtonForeground" Color="#FFF5F5F5" />
            <SolidColorBrush x:Key="ButtonForegroundPointerOver" Color="#FFF5F5F5" />
            <SolidColorBrush x:Key="ButtonForegroundPressed" Color="#E8FFFFFF" />
            <SolidColorBrush x:Key="ButtonForegroundDisabled" Color="#8FFFFFFF" />
            <SolidColorBrush x:Key="SubtleButtonForeground" Color="#FFF5F5F5" />
            <SolidColorBrush x:Key="SubtleButtonForegroundPointerOver" Color="#FFF5F5F5" />
            <SolidColorBrush x:Key="SubtleButtonForegroundPressed" Color="#E8FFFFFF" />
            <SolidColorBrush x:Key="SubtleButtonForegroundDisabled" Color="#8FFFFFFF" />

            <!-- Native input and item templates otherwise retain the app theme
                 foreground. Alias every relevant state to this widget's
                 mutable semantic brushes. -->
            <StaticResource x:Key="DefaultTextForegroundThemeBrush" ResourceKey="TextFillColorPrimaryBrush" />
            <StaticResource x:Key="TextControlForeground" ResourceKey="TextFillColorPrimaryBrush" />
            <StaticResource x:Key="TextControlForegroundPointerOver" ResourceKey="TextFillColorPrimaryBrush" />
            <StaticResource x:Key="TextControlForegroundFocused" ResourceKey="TextFillColorPrimaryBrush" />
            <StaticResource x:Key="TextControlForegroundDisabled" ResourceKey="TextFillColorDisabledBrush" />
            <StaticResource x:Key="TextControlPlaceholderForeground" ResourceKey="TextFillColorSecondaryBrush" />
            <StaticResource x:Key="TextControlPlaceholderForegroundPointerOver" ResourceKey="TextFillColorSecondaryBrush" />
            <StaticResource x:Key="TextControlPlaceholderForegroundFocused" ResourceKey="TextFillColorSecondaryBrush" />
            <StaticResource x:Key="TextControlPlaceholderForegroundDisabled" ResourceKey="TextFillColorDisabledBrush" />
            <StaticResource x:Key="GridViewItemForeground" ResourceKey="TextFillColorPrimaryBrush" />
            <StaticResource x:Key="GridViewItemForegroundPointerOver" ResourceKey="TextFillColorSecondaryBrush" />
            <StaticResource x:Key="GridViewItemForegroundSelected" ResourceKey="TextFillColorPrimaryBrush" />
            <!-- Clipboard record list colors. The foreground brush aliases the
                 widget-local semantic text color so follow-theme mode stays
                 consistent with the widget foreground preference; the card
                 brush starts at the theme card color and both colors are
                 replaced at runtime when the user picks custom colors
                 (QuickCaptureClipboardColorSettings). -->
            <SolidColorBrush x:Key="QuickCaptureClipboardItemForegroundBrush" Color="#FFF5F5F5" />
            <SolidColorBrush x:Key="QuickCaptureClipboardItemBackgroundBrush" Color="{ThemeResource CardBackgroundFillColorDefault}" />
            <SolidColorBrush x:Key="QuickCaptureClipboardItemCardThemeBrush" Color="{ThemeResource CardBackgroundFillColorDefault}" />
            <StaticResource x:Key="ListViewItemForeground" ResourceKey="QuickCaptureClipboardItemForegroundBrush" />
            <StaticResource x:Key="ListViewItemForegroundPointerOver" ResourceKey="QuickCaptureClipboardItemForegroundBrush" />
            <StaticResource x:Key="ListViewItemForegroundPressed" ResourceKey="QuickCaptureClipboardItemForegroundBrush" />
            <StaticResource x:Key="ListViewItemForegroundSelected" ResourceKey="QuickCaptureClipboardItemForegroundBrush" />
            <StaticResource x:Key="ListViewItemForegroundSelectedPointerOver" ResourceKey="QuickCaptureClipboardItemForegroundBrush" />
            <StaticResource x:Key="ListViewItemForegroundSelectedPressed" ResourceKey="QuickCaptureClipboardItemForegroundBrush" />

            <x:Double x:Key="QuickCaptureDetailActionIconSize">13</x:Double>

            <Style x:Key="QuickCaptureIconButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="22" />
                <Setter Property="Height" Value="22" />
                <Setter Property="MinWidth" Value="22" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>

            <Style x:Key="QuickCaptureItemActionButtonStyle" TargetType="Button" BasedOn="{StaticResource WidgetItemActionButtonStyle}" />

            <Style x:Key="QuickCaptureItemButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="10,8" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
            </Style>

            <Style x:Key="QuickCaptureSegmentButtonStyle" TargetType="Button">
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="MinHeight" Value="24" />
                <Setter Property="Height" Value="24" />
                <Setter Property="Padding" Value="4,0" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
                <Setter Property="FontSize" Value="12" />
            </Style>

            <Style x:Key="QuickCaptureConfirmCommandButtonStyle" TargetType="Button" BasedOn="{StaticResource WidgetConfirmCommandButtonStyle}" />

            <Style x:Key="QuickCaptureDetailIconButtonStyle" TargetType="Button" BasedOn="{StaticResource SubtleButtonStyle}">
                <Setter Property="Width" Value="28" />
                <Setter Property="Height" Value="28" />
                <Setter Property="MinWidth" Value="28" />
                <Setter Property="MinHeight" Value="28" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="CornerRadius" Value="4" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorSecondaryBrush}" />
            </Style>

            <Style x:Key="QuickCaptureDetailConfirmButtonStyle" TargetType="Button" BasedOn="{StaticResource QuickCaptureDetailIconButtonStyle}">
                <Setter Property="Width" Value="28" />
                <Setter Property="Height" Value="28" />
                <Setter Property="MinWidth" Value="28" />
                <Setter Property="MinHeight" Value="28" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>

            <Style x:Key="QuickCaptureAttachmentActionButtonStyle" TargetType="Button" BasedOn="{StaticResource SubtleButtonStyle}">
                <Setter Property="Width" Value="22" />
                <Setter Property="Height" Value="22" />
                <Setter Property="MinWidth" Value="22" />
                <Setter Property="MinHeight" Value="22" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="Background" Value="{ThemeResource WidgetOverlayInputSurfaceBrush}" />
                <Setter Property="BorderBrush" Value="{ThemeResource WidgetOverlayStrokeBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="4" />
                <Setter Property="Foreground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
            </Style>

            <Style x:Key="QuickCaptureMaterialButtonStyle" TargetType="Button" BasedOn="{StaticResource SubtleButtonStyle}">
                <Setter Property="Width" Value="28" />
                <Setter Property="Height" Value="24" />
                <Setter Property="MinWidth" Value="28" />
                <Setter Property="MinHeight" Value="24" />
                <Setter Property="Padding" Value="3" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="Transparent" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="4" />
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="46" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <controls:WidgetShell
            x:Name="QuickCaptureShell"
            Grid.RowSpan="2"
            AddRequested="AddButton_Click"
            CloseRequested="CloseButton_Click"
            DragHandlePointerMoved="DragHandle_PointerMoved"
            DragHandlePointerPressed="DragHandle_PointerPressed"
            DragHandlePointerReleased="DragHandle_PointerReleased"
            MoreRequested="MoreButton_Click"
            PositionLockRequested="PositionLockButton_Click"
            SizeLockRequested="SizeLockButton_Click"
            OverlayTitle="{Binding DisplayName}"
            IsTitleEditable="True"
            TitleDoubleTapped="QuickCaptureShell_TitleDoubleTapped"
            TitlePointerMoved="TitleBarGrid_PointerMoved"
            TitlePointerPressed="TitleBarGrid_PointerPressed"
            TitlePointerReleased="TitleBarGrid_PointerReleased"
            TitleRightTapped="TitleBarGrid_RightTapped"
            RightTapped="QuickCaptureShell_RightTapped">
            <controls:WidgetShell.ShellContent>
                <Grid
                    x:Name="ResponsiveContentGrid"
                    Padding="{Binding RootPadding}"
                    RowSpacing="{Binding RootRowSpacing}"
                    SizeChanged="ResponsiveContentGrid_SizeChanged">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="MasterColumn" Width="*" />
                <ColumnDefinition x:Name="SplitterColumn" Width="0" />
                <ColumnDefinition x:Name="DetailColumn" Width="0" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid x:Name="ListPage" Grid.Column="0" Grid.RowSpan="4" RowSpacing="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

            <Grid Grid.Row="2" Visibility="{Binding InputAreaVisibility}">
                <TextBox
                    x:Name="InputTextBox"
                    Visibility="Collapsed"
                    Height="{Binding InputHeight}"
                    MinHeight="{Binding InputHeight}"
                    Padding="{Binding InputPadding}"
                    FontSize="{Binding TextSize}"
                    PlaceholderText="{Binding InputPlaceholderText}"
                    Style="{StaticResource WidgetBorderlessInputTextBoxStyle}"
                    Text="{Binding InputText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    KeyDown="InputTextBox_KeyDown" />
                <Button
                    x:Name="ExpandInputButton"
                    Visibility="Collapsed"
                    Click="ExpandInputButton_Click"
                    Width="{Binding InputButtonSize}"
                    Height="{Binding InputButtonSize}"
                    MinWidth="{Binding InputButtonSize}"
                    MinHeight="{Binding InputButtonSize}"
                    Style="{StaticResource WidgetCompactIconButtonStyle}"
                    ToolTipService.ToolTip="{Binding ExpandInputTooltipText}">
                    <FontIcon FontSize="{Binding InputActionIconSize}" Glyph="&#xE740;" />
                </Button>
            </Grid>

            <Border Grid.Row="1" Height="1" Margin="0,1"
                Visibility="{Binding InputAreaVisibility}" />

            <Grid Grid.Row="0" ColumnSpacing="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid
                    x:Name="TabHost"
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    Visibility="{Binding TabBarVisibility}">
                    <toolkit:Segmented
                        x:Name="QuickCaptureViewSegmented"
                        MinHeight="{Binding SegmentHeight}"
                        FontSize="{Binding SegmentTextSize}"
                        HorizontalAlignment="Stretch"
                        Loaded="QuickCaptureViewSegmented_Loaded"
                        SelectionChanged="QuickCaptureViewSegmented_SelectionChanged"
                        SelectedIndex="0"
                        SizeChanged="QuickCaptureViewSegmented_SizeChanged">
                        <toolkit:SegmentedItem
                            AllowDrop="True"
                            DragOver="QuickCaptureTab_DragOver"
                            Drop="QuickCaptureTab_Drop"
                            Tag="Records"
                            Visibility="{Binding RecordsTabVisibility}"
                            ToolTipService.ToolTip="{Binding RecordsTabText}">
                            <TextBlock
                                MinWidth="0"
                                VerticalAlignment="Center"
                                FontSize="{Binding SegmentTextSize}"
                                MaxLines="1"
                                Text="{Binding RecordsTabText}"
                                TextAlignment="Center"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap" />
                        </toolkit:SegmentedItem>
                        <toolkit:SegmentedItem
                            AllowDrop="True"
                            DragOver="QuickCaptureTab_DragOver"
                            Drop="QuickCaptureTab_Drop"
                            Tag="Pinned"
                            Visibility="{Binding PinnedTabVisibility}"
                            ToolTipService.ToolTip="{Binding PinnedTabText}">
                            <TextBlock
                                MinWidth="0"
                                VerticalAlignment="Center"
                                FontSize="{Binding SegmentTextSize}"
                                MaxLines="1"
                                Text="{Binding PinnedTabText}"
                                TextAlignment="Center"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap" />
                        </toolkit:SegmentedItem>
                        <toolkit:SegmentedItem
                            AllowDrop="True"
                            DragOver="QuickCaptureTab_DragOver"
                            Drop="QuickCaptureTab_Drop"
                            Tag="Recent"
                            Visibility="{Binding RecentTabVisibility}"
                            ToolTipService.ToolTip="{Binding RecentTabText}">
                            <TextBlock
                                MinWidth="0"
                                VerticalAlignment="Center"
                                FontSize="{Binding SegmentTextSize}"
                                MaxLines="1"
                                Text="{Binding RecentTabText}"
                                TextAlignment="Center"
                                TextTrimming="CharacterEllipsis"
                                TextWrapping="NoWrap" />
                        </toolkit:SegmentedItem>
                    </toolkit:Segmented>
                </Grid>
                <Button
                    x:Name="SearchButton"
                    Grid.Column="1"
                    Click="SearchButton_Click"
                    Width="22"
                    Height="22"
                    MinWidth="22"
                    Style="{StaticResource QuickCaptureIconButtonStyle}"
                    ToolTipService.ToolTip="{Binding SearchPlaceholderText}"
                    Visibility="{Binding SearchButtonVisibility}">
                    <FontIcon FontSize="12" Glyph="&#xE721;" />
                </Button>
                <TextBox
                    x:Name="SearchTextBox"
                    Grid.Column="0"
                    Height="{Binding InputHeight}"
                    MinHeight="{Binding InputHeight}"
                    Padding="{Binding InputPadding}"
                    FontSize="{Binding TextSize}"
                    KeyDown="SearchTextBox_KeyDown"
                    PlaceholderText="{Binding SearchPlaceholderText}"
                    Style="{StaticResource WidgetSearchTextBoxStyle}"
                    Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    Visibility="{Binding SearchBoxVisibility}" />
                <Button
                    x:Name="CloseSearchButton"
                    Grid.Column="1"
                    Click="CloseSearchButton_Click"
                    Width="{Binding InputButtonSize}"
                    Height="{Binding InputButtonSize}"
                    MinWidth="{Binding InputButtonSize}"
                    MinHeight="{Binding InputButtonSize}"
                    Style="{StaticResource WidgetCompactIconButtonStyle}"
                    Visibility="{Binding SearchBoxVisibility}">
                    <FontIcon FontSize="{Binding InputActionIconSize}" Glyph="&#xE711;" />
                </Button>
            </Grid>

            <TextBlock
                Grid.Row="2"
                Margin="2,-2,6,0"
                FontSize="{Binding SecondaryTextSize}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{Binding SearchScopeText}"
                TextAlignment="Center"
                TextTrimming="CharacterEllipsis"
                Visibility="{Binding SearchScopeVisibility}" />

            <TextBlock
                Grid.Row="2"
                Margin="2,-2,6,0"
                FontSize="{Binding SecondaryTextSize}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{Binding RecentCaptureStatusText}"
                TextAlignment="Center"
                TextTrimming="CharacterEllipsis"
                Visibility="{Binding RecentCaptureStatusVisibility}" />

            <Grid Grid.Row="3">
                <Grid
                    x:Name="EmptyStateHost"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Visibility="{Binding EmptyStateVisibility}">
                    <StackPanel
                        x:Name="EmptyState"
                        Margin="0,-8,0,0"
                        Spacing="7"
                        Style="{StaticResource WidgetEmptyStatePanelStyle}">
                        <FontIcon
                            x:Name="EmptyStateIcon"
                            Margin="0,0,0,2"
                            FontSize="{Binding EmptyIconSize}"
                            Glyph="&#xE8A5;"
                            Style="{StaticResource WidgetEmptyStateIconStyle}" />
                        <TextBlock
                            FontSize="{Binding TitleTextSize}"
                            FontWeight="SemiBold"
                            MaxLines="1"
                            Style="{StaticResource WidgetEmptyStateTitleTextStyle}"
                            Text="{Binding EmptyStateTitle}"
                            TextWrapping="NoWrap" />
                        <TextBlock
                            FontSize="{Binding TextSize}"
                            MaxLines="2"
                            Style="{StaticResource WidgetEmptyStateDescriptionTextStyle}"
                            Text="{Binding EmptyStateText}" />
                        <Button
                            Click="EnableRecentCaptureButton_Click"
                            FontSize="{Binding SegmentTextSize}"
                            HorizontalAlignment="Center"
                            Style="{StaticResource QuickCaptureSegmentButtonStyle}"
                            Content="{Binding EnableRecentCaptureText}"
                            Visibility="{Binding RecentCaptureActionVisibility}" />
                    </StackPanel>
                </Grid>

                <ListView
                    x:Name="ItemsListView"
                    AllowDrop="True"
                    CanDragItems="True"
                    CanReorderItems="False"
                    Background="Transparent"
                    HorizontalContentAlignment="Stretch"
                    ItemsSource="{Binding VisibleItemsSource}"
                    DragItemsCompleted="ItemsListView_DragItemsCompleted"
                    DragItemsStarting="ItemsListView_DragItemsStarting"
                    KeyDown="ItemsListView_KeyDown"
                    PointerPressed="ItemsListView_PointerPressed"
                    PointerMoved="ItemsListView_PointerMoved"
                    PointerReleased="ItemsListView_PointerReleased"
                    PointerCaptureLost="ItemsListView_PointerCaptureLost"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    ScrollViewer.HorizontalScrollMode="Disabled"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    SelectionMode="Single"
                    SelectionChanged="ItemsListView_SelectionChanged"
                    Visibility="{Binding ListVisibility}">
                    <ListView.Resources>
                        <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="Transparent" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundPressed" Color="Transparent" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelected" Color="Transparent" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver" Color="Transparent" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPressed" Color="Transparent" />
                        <SolidColorBrush x:Key="ListViewItemBackgroundSelectedDisabled" Color="Transparent" />
                    </ListView.Resources>
                    <ListView.Header>
                        <Button
                            x:Name="AddNoteCardButton"
                            Margin="{Binding AddCardMargin}"
                            MinHeight="{Binding AddCardMinHeight}"
                            Padding="{Binding AddCardPadding}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            Click="AddNoteCardButton_Click"
                            CornerRadius="4"
                            Visibility="{Binding InputAreaVisibility}">
                            <Grid ColumnSpacing="4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="32" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <FontIcon
                                    Grid.Column="0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    FontSize="{Binding PrimaryIconSize}"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Glyph="&#xE710;" />
                                <TextBlock
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    FontSize="{Binding TextSize}"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{Binding AddNoteText}" />
                            </Grid>
                        </Button>
                    </ListView.Header>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="MinHeight" Value="0" />
                            <Setter Property="UseSystemFocusVisuals" Value="False" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="HorizontalAlignment" Value="Stretch" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="VerticalContentAlignment" Value="Stretch" />
                            <Setter Property="Transitions">
                                <Setter.Value>
                                    <TransitionCollection>
                                        <AddDeleteThemeTransition />
                                        <ReorderThemeTransition />
                                    </TransitionCollection>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid
                                x:Name="QuickCaptureItemRoot"
                                AllowDrop="True"
                                HorizontalAlignment="Stretch"
                                Margin="{Binding ElementName=ItemsListView, Path=DataContext.ItemMargin}"
                                Padding="{Binding ElementName=ItemsListView, Path=DataContext.ItemPadding}"
                                Background="Transparent"
                                DataContextChanged="QuickCaptureItem_DataContextChanged"
                                DoubleTapped="QuickCaptureItem_DoubleTapped"
                                DragLeave="QuickCaptureItem_DragLeave"
                                DragOver="QuickCaptureItem_DragOver"
                                Drop="QuickCaptureItem_Drop"
                                PointerEntered="QuickCaptureItem_PointerEntered"
                                PointerExited="QuickCaptureItem_PointerExited"
                                RightTapped="QuickCaptureItem_RightTapped"
                                Tapped="QuickCaptureItem_Tapped">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="28" />
                                </Grid.ColumnDefinitions>
                                <Border
                                    x:Name="ItemMaterialBackground"
                                    Grid.ColumnSpan="2"
                                    Background="{StaticResource QuickCaptureClipboardItemBackgroundBrush}"
                                    BorderThickness="0"
                                    CornerRadius="4"
                                    Margin="{Binding ElementName=ItemsListView, Path=DataContext.ItemChromeMargin}" />
                                <Border
                                    x:Name="ItemHoverBackground"
                                    Grid.ColumnSpan="2"
                                    Margin="{Binding ElementName=ItemsListView, Path=DataContext.ItemChromeMargin}"
                                    Style="{StaticResource WidgetRowHoverBackgroundStyle}" />
                                <Border
                                    Grid.ColumnSpan="2"
                                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                                    CornerRadius="4"
                                    Margin="{Binding ElementName=ItemsListView, Path=DataContext.ItemChromeMargin}"
                                    Opacity="0.18"
                                    Visibility="{Binding CopySelectionVisibility}" />
                                <Border
                                    Grid.ColumnSpan="2"
                                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                                    CornerRadius="4"
                                    Margin="{Binding ElementName=ItemsListView, Path=DataContext.ItemChromeMargin}"
                                    Opacity="0.10"
                                    Visibility="{Binding DetailSelectionVisibility}" />
                                <Border
                                    Width="2"
                                    Margin="{Binding ElementName=ItemsListView, Path=DataContext.ItemChromeMargin}"
                                    HorizontalAlignment="Left"
                                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                                    CornerRadius="1"
                                    Visibility="{Binding DetailSelectionVisibility}" />
                                    <StackPanel
                                        x:Name="ItemContentHost"
                                        Grid.Column="0"
                                        Margin="0"
                                        MinWidth="0"
                                        Spacing="0"
                                        VerticalAlignment="Center">
                                    <TextBlock
                                        FontSize="{Binding TextSize}"
                                        FontWeight="SemiBold"
                                        MaxLines="1"
                                        Text="{Binding Title}"
                                        TextTrimming="CharacterEllipsis"
                                        Visibility="Collapsed" />
                                    <ScrollViewer
                                        MaxWidth="360"
                                        HorizontalAlignment="Left"
                                        HorizontalScrollBarVisibility="Auto"
                                        HorizontalScrollMode="Enabled"
                                        VerticalScrollBarVisibility="Disabled"
                                        VerticalScrollMode="Disabled"
                                        Visibility="{Binding ImageAttachmentsVisibility}">
                                        <ItemsControl ItemsSource="{Binding ImageAttachmentItemsSource}">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal" Spacing="5" />
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="viewModels:TodoAttachmentViewModel">
                                                    <Grid
                                                        Width="116"
                                                        Height="72"
                                                        Loaded="QuickCaptureAttachmentPreview_Loaded">
                                                        <Border
                                                            Padding="2"
                                                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="4">
                                                            <Image Source="{x:Bind Thumbnail, Mode=OneWay}" Stretch="Uniform" />
                                                        </Border>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                    <TextBlock
                                        x:Name="ItemTextBlock"
                                        FontSize="{Binding TextSize}"
                                        CharacterSpacing="8"
                                        LineHeight="{Binding ElementName=ItemsListView, Path=DataContext.ItemTextLineHeight}"
                                        LineStackingStrategy="BlockLineHeight"
                                        MaxLines="{Binding ElementName=ItemsListView, Path=DataContext.ItemPreviewLineCount}"
                                        Text="{Binding DisplayText}"
                                        TextTrimming="CharacterEllipsis"
                                        TextWrapping="Wrap"
                                        Visibility="{Binding TextPreviewVisibility}" />
                                    <TextBlock
                                        FontSize="{Binding SecondaryTextSize}"
                                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                        LineHeight="{Binding ElementName=ItemsListView, Path=DataContext.ItemMetaLineHeight}"
                                        LineStackingStrategy="BlockLineHeight"
                                        Text="{Binding CreatedAtText}"
                                        Visibility="{Binding ElementName=ItemsListView, Path=DataContext.CreatedTimeVisibility}" />
                                    <StackPanel Orientation="Horizontal" Spacing="3" Visibility="{Binding AttachmentSummaryVisibility}">
                                        <FontIcon FontSize="{Binding TypeIconSize}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" Glyph="&#xE723;" />
                                        <TextBlock FontSize="{Binding SecondaryTextSize}" Foreground="{ThemeResource TextFillColorTertiaryBrush}" Text="{Binding AttachmentSummaryText}" />
                                    </StackPanel>
                                    </StackPanel>
                                <Button
                                    x:Name="CopyItemButton"
                                    Grid.Column="1"
                                    Width="28"
                                    Height="28"
                                    MinWidth="28"
                                    MinHeight="28"
                                    Padding="0"
                                    Click="CopyItemButton_Click"
                                    Style="{StaticResource SubtleButtonStyle}"
                                    Tag="{Binding}"
                                    VerticalAlignment="Center">
                                    <FontIcon FontSize="{Binding ActionIconSize}" Glyph="&#xE8C8;" />
                                </Button>
                                <Border
                                    x:Name="ItemActionHost"
                                    Grid.ColumnSpan="2"
                                    Canvas.ZIndex="2"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    Margin="0,0,34,0"
                                    HorizontalAlignment="Right"
                                    Style="{StaticResource WidgetHoverActionHostStyle}"
                                    VerticalAlignment="Center"
                                    Visibility="Collapsed">
                                    <StackPanel
                                        x:Name="ItemActionButtons"
                                        HorizontalAlignment="Right"
                                        Orientation="Horizontal"
                                        Spacing="4"
                                        VerticalAlignment="Center">
                                        <Button
                                            x:Name="MovePinnedItemUpButton"
                                            Click="MovePinnedItemUpButton_Click"
                                            IsEnabled="{Binding CanMovePinnedUp}"
                                            AutomationProperties.Name="{Binding MoveUpTooltip}"
                                            Style="{StaticResource QuickCaptureItemActionButtonStyle}"
                                            Tag="{Binding}"
                                            ToolTipService.ToolTip="{Binding MoveUpTooltip}"
                                            Visibility="{Binding PinnedSortControlsVisibility}">
                                            <FontIcon FontSize="11" Glyph="&#xE70E;" />
                                        </Button>
                                        <Button
                                            x:Name="MovePinnedItemDownButton"
                                            Click="MovePinnedItemDownButton_Click"
                                            IsEnabled="{Binding CanMovePinnedDown}"
                                            AutomationProperties.Name="{Binding MoveDownTooltip}"
                                            Style="{StaticResource QuickCaptureItemActionButtonStyle}"
                                            Tag="{Binding}"
                                            ToolTipService.ToolTip="{Binding MoveDownTooltip}"
                                            Visibility="{Binding PinnedSortControlsVisibility}">
                                            <FontIcon FontSize="11" Glyph="&#xE70D;" />
                                        </Button>
                                        <Button
                                            x:Name="SaveRecentItemButton"
                                            Click="SaveRecentItemButton_Click"
                                            Style="{StaticResource QuickCaptureItemActionButtonStyle}"
                                            Tag="{Binding}">
                                            <FontIcon FontSize="11" Glyph="&#xE74E;" />
                                        </Button>
                                        <Button
                                            x:Name="PinItemButton"
                                            Click="PinItemButton_Click"
                                            AutomationProperties.Name="{Binding PinTooltip}"
                                            Style="{StaticResource QuickCaptureItemActionButtonStyle}"
                                            Tag="{Binding}"
                                            ToolTipService.ToolTip="{Binding PinTooltip}">
                                            <controls:PinStateIcon Width="13" Height="13" IsPinned="{Binding IsPinned}" />
                                        </Button>
                                        <Button
                                            x:Name="DeleteItemButton"
                                            Click="DeleteItemButton_Click"
                                            Style="{StaticResource QuickCaptureItemActionButtonStyle}"
                                            Tag="{Binding}">
                                            <FontIcon FontSize="11" Glyph="&#xE74D;" />
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <Canvas
                    x:Name="SelectionOverlay"
                    Background="Transparent"
                    IsHitTestVisible="False">
                    <Border
                        x:Name="SelectionRectangle"
                        Width="0"
                        Height="0"
                        BorderThickness="1"
                        CornerRadius="0"
                        Visibility="Collapsed" />
                </Canvas>

                <Grid
                    Background="Transparent"
                    IsHitTestVisible="False"
                    Visibility="{Binding ViewSwitchLoadingVisibility}">
                    <Border
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Padding="9"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Opacity="0.92">
                        <ProgressRing
                            Width="20"
                            Height="20"
                            IsActive="{Binding IsSwitchingView}" />
                    </Border>
                </Grid>
            </Grid>
            </Grid>

            <toolkit:GridSplitter
                x:Name="PaneSplitter"
                Grid.Column="1"
                Grid.RowSpan="4"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Margin="-3,0"
                AutomationProperties.Name="{Binding DetailSplitterText}"
                Canvas.ZIndex="9"
                DoubleTapped="PaneSplitter_DoubleTapped"
                DragIncrement="1"
                IsTabStop="True"
                IsThumbVisible="True"
                KeyboardIncrement="8"
                KeyUp="PaneSplitter_KeyUp"
                ManipulationCompleted="PaneSplitter_ManipulationCompleted"
                ResizeBehavior="PreviousAndNext"
                ResizeDirection="Columns"
                Style="{StaticResource WidgetMasterDetailSplitterStyle}"
                ToolTipService.ToolTip="{Binding DetailSplitterText}"
                Visibility="Collapsed" />

            <Grid
                x:Name="DetailPage"
                Grid.Column="2"
                Grid.RowSpan="4"
                Canvas.ZIndex="8"
                Background="Transparent"
                Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border
                    x:Name="DetailHeader"
                    Grid.Row="0"
                    MinHeight="40"
                    Padding="4,2"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="0">
                    <Grid>
                        <Border
                            x:Name="DetailHeaderAcrylicSurface"
                            Background="{ThemeResource SystemControlAcrylicElementBrush}"
                            BorderBrush="{ThemeResource WidgetLayerStrokeBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            IsHitTestVisible="False"
                            Opacity="0.42" />
                        <Grid MinHeight="36" Padding="2,0" ColumnSpacing="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="28" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="28" />
                            <ColumnDefinition Width="28" />
                            <ColumnDefinition Width="28" />
                            <ColumnDefinition Width="28" />
                        </Grid.ColumnDefinitions>
                        <Button
                            x:Name="DetailBackButton"
                            Click="DetailBackButton_Click"
                            Style="{StaticResource QuickCaptureDetailConfirmButtonStyle}"
                            ToolTipService.ToolTip="{Binding DetailBackText}"
                            VerticalAlignment="Center">
                            <FontIcon
                                FontSize="{StaticResource QuickCaptureDetailActionIconSize}"
                                Glyph="&#xE73E;" />
                        </Button>
                        <TextBox
                            x:Name="DetailTitleTextBox"
                            Grid.Column="1"
                            Visibility="Collapsed" />
                        <TextBlock
                            x:Name="DetailTimestampText"
                            Grid.Column="1"
                            Margin="6,0,4,0"
                            VerticalAlignment="Center"
                            FontSize="{Binding DetailHeaderTextSize}"
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                            MaxLines="1"
                            TextTrimming="CharacterEllipsis"
                            Visibility="{Binding CreatedTimeVisibility}" />
                        <TextBlock
                            x:Name="DetailReadOnlyText"
                            Grid.Column="2"
                            Margin="4,0"
                            VerticalAlignment="Center"
                            FontSize="11"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            MaxLines="1"
                            Text="{Binding DetailReadOnlyText}"
                            TextTrimming="CharacterEllipsis"
                            Visibility="Collapsed" />
                        <Grid Grid.Column="3">
                            <Button
                                x:Name="DetailEditButton"
                                Click="DetailEditButton_Click"
                                Style="{StaticResource QuickCaptureDetailIconButtonStyle}"
                                ToolTipService.ToolTip="{Binding DetailEditText}">
                                <FontIcon FontSize="{StaticResource QuickCaptureDetailActionIconSize}" Glyph="&#xE70F;" />
                            </Button>
                            <Button
                                x:Name="DetailDoneButton"
                                Click="DetailDoneButton_Click"
                                Style="{StaticResource QuickCaptureDetailIconButtonStyle}"
                                ToolTipService.ToolTip="{Binding DetailDoneText}"
                                Visibility="Collapsed">
                                <FontIcon FontSize="{StaticResource QuickCaptureDetailActionIconSize}" Glyph="&#xE73E;" />
                            </Button>
                        </Grid>
                        <Button
                            x:Name="DetailPinButton"
                            Grid.Column="4"
                            Click="DetailPinButton_Click"
                            Style="{StaticResource QuickCaptureDetailIconButtonStyle}"
                            VerticalAlignment="Center">
                            <controls:PinStateIcon x:Name="DetailPinIcon" Width="13" Height="13" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </Button>
                        <Button
                            Grid.Column="5"
                            Click="DetailCopyButton_Click"
                            Style="{StaticResource QuickCaptureDetailIconButtonStyle}"
                            ToolTipService.ToolTip="{Binding DetailCopyText}"
                            VerticalAlignment="Center">
                            <FontIcon FontSize="{StaticResource QuickCaptureDetailActionIconSize}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Glyph="&#xE8C8;" />
                        </Button>
                        <Button
                            Grid.Column="6"
                            Click="DetailDeleteButton_Click"
                            Style="{StaticResource QuickCaptureDetailIconButtonStyle}"
                            ToolTipService.ToolTip="{Binding DetailDeleteText}"
                            VerticalAlignment="Center">
                            <FontIcon FontSize="{StaticResource QuickCaptureDetailActionIconSize}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Glyph="&#xE74D;" />
                        </Button>
                        </Grid>
                    </Grid>
                </Border>

                <Grid x:Name="DetailContent" Grid.Row="1" Margin="4,0,4,6" RowSpacing="6">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Border
                        x:Name="DetailMaterialSurface"
                        Grid.Row="0"
                        MinHeight="132"
                        Padding="8,2,8,8"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="Transparent"
                        BorderThickness="0"
                        CornerRadius="8">
                        <Grid RowSpacing="3">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <ScrollViewer
                                x:Name="DetailAttachmentScroller"
                                Grid.Row="0"
                                MaxHeight="118"
                                HorizontalScrollBarVisibility="Auto"
                                HorizontalScrollMode="Enabled"
                                VerticalScrollBarVisibility="Disabled"
                                VerticalScrollMode="Disabled"
                                Visibility="Collapsed">
                                <ItemsControl x:Name="DetailAttachmentsList">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" Spacing="5" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="viewModels:TodoAttachmentViewModel">
                                            <Grid Width="116" Height="96" Loaded="QuickCaptureAttachmentPreview_Loaded">
                                                <Button
                                                    Padding="3"
                                                    HorizontalContentAlignment="Stretch"
                                                    VerticalContentAlignment="Stretch"
                                                    Click="DetailOpenAttachmentButton_Click"
                                                    Style="{StaticResource SubtleButtonStyle}">
                                                    <Grid RowSpacing="2">
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="68" />
                                                            <RowDefinition Height="*" />
                                                        </Grid.RowDefinitions>
                                                        <Border Background="{ThemeResource SubtleFillColorSecondaryBrush}" CornerRadius="3">
                                                            <Grid>
                                                                <Image Source="{x:Bind Thumbnail, Mode=OneWay}" Stretch="Uniform" Visibility="{x:Bind ThumbnailVisibility, Mode=OneWay}" />
                                                                <FontIcon FontSize="24" Glyph="{x:Bind Glyph, Mode=OneWay}" Visibility="{x:Bind FileIconVisibility, Mode=OneWay}" />
                                                            </Grid>
                                                        </Border>
                                                        <TextBlock Grid.Row="1" FontSize="{Binding ElementName=ItemsListView, Path=DataContext.SecondaryTextSize}" MaxLines="1" Text="{x:Bind DisplayName, Mode=OneWay}" TextTrimming="CharacterEllipsis" />
                                                    </Grid>
                                                </Button>
                                                <StackPanel
                                                    Margin="3"
                                                    HorizontalAlignment="Right"
                                                    VerticalAlignment="Top"
                                                    Canvas.ZIndex="2"
                                                    Orientation="Horizontal"
                                                    Spacing="3">
                                                    <Button
                                                        AutomationProperties.Name="{Binding ElementName=ItemsListView, Path=DataContext.DetailCopyImageText}"
                                                        Click="QuickCaptureAttachmentCopyButton_Click"
                                                        Style="{StaticResource QuickCaptureAttachmentActionButtonStyle}"
                                                        ToolTipService.ToolTip="{Binding ElementName=ItemsListView, Path=DataContext.DetailCopyImageText}"
                                                        Visibility="{x:Bind ImageCopyVisibility, Mode=OneWay}">
                                                        <FontIcon FontSize="10" Glyph="&#xE8C8;" />
                                                    </Button>
                                                     <Button
                                                         AutomationProperties.Name="{Binding ElementName=ItemsListView, Path=DataContext.DetailRemoveAttachmentText}"
                                                         Click="DetailRemoveAttachmentButton_Click"
                                                         Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                                         Style="{StaticResource QuickCaptureAttachmentActionButtonStyle}"
                                                         ToolTipService.ToolTip="{Binding ElementName=ItemsListView, Path=DataContext.DetailRemoveAttachmentText}"
                                                         Visibility="{Binding ElementName=DetailMarkdownEditor, Path=Visibility}">
                                                        <FontIcon FontSize="10" Glyph="&#xE74D;" />
                                                    </Button>
                                                </StackPanel>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                            <controls:MarkdownDocumentView
                                x:Name="DetailMarkdownView"
                                Grid.Row="2"
                                MinHeight="80"
                                FontSize="{Binding TextSize}"
                                Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                Margin="8,6,0,6"
                                TaskToggleRequested="DetailMarkdownView_TaskToggleRequested"
                                Visibility="Collapsed" />
                            <controls:MarkdownSourceEditor
                                x:Name="DetailMarkdownEditor"
                                Grid.Row="2"
                                MinHeight="80"
                                CommitRequested="DetailMarkdownEditor_CommitRequested"
                                CancelRequested="DetailMarkdownEditor_CancelRequested"
                                EditorFontSize="{Binding TextSize}"
                                EditorEnterBehavior="{Binding EditorEnterBehavior}"
                                EnableCtrlSSaveShortcut="True"
                                PlaceholderText="{Binding DetailBodyPlaceholderText}"
                                Visibility="Collapsed" />
                            <Button
                                x:Name="DetailAddFileButton"
                                Grid.Row="1"
                                MinHeight="32"
                                Padding="6,3"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                Click="DetailAddFileButton_Click"
                                Style="{StaticResource SubtleButtonStyle}">
                                <StackPanel Orientation="Horizontal" Spacing="7">
                                    <FontIcon FontSize="{Binding ActionIconSize}" Glyph="&#xE723;" />
                                    <TextBlock VerticalAlignment="Center" FontSize="{Binding TextSize}" Text="{Binding AddFileText}" />
                                </StackPanel>
                            </Button>
                        </Grid>
                    </Border>

                    <StackPanel x:Name="DetailMaterialPalette" Grid.Row="1" Margin="2,0,0,0" Orientation="Horizontal" Spacing="5" Visibility="Collapsed">
                            <Button x:Name="DefaultMaterialButton" Click="MaterialButton_Click" Style="{StaticResource QuickCaptureMaterialButtonStyle}" Tag="Default" ToolTipService.ToolTip="{Binding MaterialDefaultText}"><Border Width="18" Height="12" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" CornerRadius="3" /></Button>
                            <Button x:Name="PaperMaterialButton" Click="MaterialButton_Click" Style="{StaticResource QuickCaptureMaterialButtonStyle}" Tag="Paper" ToolTipService.ToolTip="{Binding MaterialPaperText}"><Border Width="18" Height="12" Background="#FFF4EEE2" CornerRadius="3" /></Button>
                            <Button x:Name="YellowMaterialButton" Click="MaterialButton_Click" Style="{StaticResource QuickCaptureMaterialButtonStyle}" Tag="StickyYellow" ToolTipService.ToolTip="{Binding MaterialYellowText}"><Border Width="18" Height="12" Background="#FFFFD96A" CornerRadius="3" /></Button>
                            <Button x:Name="RoseMaterialButton" Click="MaterialButton_Click" Style="{StaticResource QuickCaptureMaterialButtonStyle}" Tag="Rose" ToolTipService.ToolTip="{Binding MaterialRoseText}"><Border Width="18" Height="12" Background="#FFF2A7B8" CornerRadius="3" /></Button>
                            <Button x:Name="MintMaterialButton" Click="MaterialButton_Click" Style="{StaticResource QuickCaptureMaterialButtonStyle}" Tag="Mint" ToolTipService.ToolTip="{Binding MaterialMintText}"><Border Width="18" Height="12" Background="#FFA8D8B5" CornerRadius="3" /></Button>
                            <Button x:Name="BlueMaterialButton" Click="MaterialButton_Click" Style="{StaticResource QuickCaptureMaterialButtonStyle}" Tag="MistBlue" ToolTipService.ToolTip="{Binding MaterialBlueText}"><Border Width="18" Height="12" Background="#FFA9C7E8" CornerRadius="3" /></Button>
                    </StackPanel>
                </Grid>

                <Grid
                    x:Name="DetailEmptyState"
                    Grid.RowSpan="3"
                    Canvas.ZIndex="10"
                    Background="Transparent"
                    Visibility="Collapsed">
                    <StackPanel
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        MaxWidth="240"
                        Spacing="8">
                        <FontIcon
                            HorizontalAlignment="Center"
                            FontSize="26"
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                            Glyph="&#xE70F;" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            FontSize="{Binding TextSize}"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="{Binding DetailNoSelectionText}"
                            TextAlignment="Center"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>

            </Grid>
                </Grid>
            </controls:WidgetShell.ShellContent>
        </controls:WidgetShell>

        <Border
            x:Name="StatusToast"
            Grid.Row="1"
            Canvas.ZIndex="10"
            Margin="12,0,12,12"
            Style="{StaticResource WidgetCompactToastStyle}">
            <StackPanel
                Orientation="Horizontal"
                Spacing="10">
                <TextBlock
                    x:Name="StatusToastText"
                    FontSize="{Binding TextSize}"
                    MaxLines="2"
                    TextAlignment="Center"
                    TextTrimming="CharacterEllipsis"
                    TextWrapping="Wrap"
                    VerticalAlignment="Center" />
                <Button
                    x:Name="StatusToastActionButton"
                    Click="StatusToastActionButton_Click"
                    Style="{StaticResource WidgetToastActionButtonStyle}"
                    Visibility="Collapsed" />
            </StackPanel>
        </Border>

        <controls:WidgetInlineEditor
            x:Name="QuickCaptureInlineEditor"
            Grid.Row="1"
            Canvas.ZIndex="20"
            Margin="10,6,6,8"
            CancelRequested="EditCancelButton_Click"
            CommandFontSize="{Binding SegmentTextSize}"
            EditorFontSize="{Binding TextSize}"
            EditorKeyDown="EditTextBox_KeyDown"
            SaveRequested="EditSaveButton_Click"
            TitleFontSize="{Binding TextSize}"
            Visibility="Collapsed">
        </controls:WidgetInlineEditor>

        <Grid
            x:Name="ResizeGrid"
            Grid.RowSpan="2"
            IsHitTestVisible="True">
            <Border x:Name="ResizeTop" Height="6" VerticalAlignment="Top" Background="Transparent" Tag="Top" PointerPressed="ResizeBorder_PointerPressed" />
            <Border x:Name="ResizeBottom" Height="6" VerticalAlignment="Bottom" Background="Transparent" Tag="Bottom" PointerPressed="ResizeBorder_PointerPressed" />
            <Border x:Name="ResizeLeft" Width="6" HorizontalAlignment="Left" Background="Transparent" Tag="Left" PointerPressed="ResizeBorder_PointerPressed" />
            <Border x:Name="ResizeRight" Width="6" HorizontalAlignment="Right" Background="Transparent" Tag="Right" PointerPressed="ResizeBorder_PointerPressed" />
            <Border x:Name="ResizeTopLeft" Width="10" Height="10" HorizontalAlignment="Left" VerticalAlignment="Top" Background="Transparent" Tag="TopLeft" PointerPressed="ResizeBorder_PointerPressed" />
            <Border x:Name="ResizeTopRight" Width="10" Height="10" HorizontalAlignment="Right" VerticalAlignment="Top" Background="Transparent" Tag="TopRight" PointerPressed="ResizeBorder_PointerPressed" />
            <Border x:Name="ResizeBottomLeft" Width="10" Height="10" HorizontalAlignment="Left" VerticalAlignment="Bottom" Background="Transparent" Tag="BottomLeft" PointerPressed="ResizeBorder_PointerPressed" />
            <Border x:Name="ResizeBottomRight" Width="10" Height="10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Background="Transparent" Tag="BottomRight" PointerPressed="ResizeBorder_PointerPressed" />
        </Grid>
    </Grid>
</views:WidgetWindowBase>

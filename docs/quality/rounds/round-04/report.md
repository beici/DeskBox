# DeskBox 迭代 R4 轮报告

- 轮次：R4（2026-08-30）｜ 分支 `wip/fix-bug` ｜ 本轮提交：驱动脚本扩展 + R4 文档
- 主题：**补充批次 GUI 场景实测** + **DEF-004 DWM/资源剖析（S2 布局编辑 50 次分段采样）** + 候选 1 真 A/B 对比

## 一、重要过程更正（诚实性声明）

R3 报告的 S3 数据（dropped=3/611）经 R4 复查确认**来自候选 1 之前的二进制**：运行实例（05:23 启动）早于候选 1 构建（05:29:45）。本轮通过「重启加载候选 1 → 重跑同序列」完成真正的 A/B 对比（见下表）。R3 报告相应表述以此更正为准；该事件也固化了流程规则：**运行时实测前必须核对进程加载的二进制时间戳**。

## 二、候选 1（Win11 合成器动画）三组 A/B 数据

| 构建 | S3 五连悬停（10 次动画） | dropped | 60fps 红线 |
|---|---|---|---|
| 1.4.8 现场版（修复前） | 日志记录 | 2–6/60（3–10%） | ❌ |
| R1 修复版（ToolTip 豁免 + 每帧分配消除，无候选 1） | R3 实测 | 3/611（0.49%） | ✅ |
| **R1 + 候选 1（合成器接管全部版本）** | **R4 实测（本轮）** | **0（0%）**，maxFrame 6.8–7.9ms | ✅ |

候选 1 增量效果实测确认：合成期 DP 写归零消除了 R1 版残余的 0.49% 掉帧。

## 三、补充批次 GUI 场景实测结果

| 任务 | 方法 | 结果 |
|---|---|---|
| **C1 点击标题闪烁** | 点击「AI 开发」标题前后全屏连拍，对「工具」胶囊与「办公文档」格子区域做像素 diff（LockBits，阈值 24） | **changed=0 / maxDelta=0（点击瞬间与稳态均零像素变化）**——无闪烁，像素级闭环 |
| **B1 边距手动输入** | 右键标题 → More 菜单 →「边距设置…」→ 对话框实测 | **全链通过**：对话框渲染正确（统一/分边切换、初值=实时派生 200px）；输入 66 → 格子即时移动（所见即所得）；点击取消 → **格子精确恢复原位**（区域 diff 29.44% 即恢复位移证据）；双向同步由初值派生机制证实 |
| **B3 标题对齐** | More 菜单 →「标题与图标」子菜单 | **子菜单完整呈现**（左对齐✓/居中/右对齐/批量对齐/图标三向入口）；居中对齐实测标题移至标题栏中央、无截断；已恢复左对齐。图标 FileOpenPicker 路径留人工复验（系统对话框坐标因人而异） |
| **B2 随记记录配色** | 右键随记标题 → More 菜单 | **部分完成**：菜单结构（B1/B3 项）验证通过，但「记录配色」子菜单在两次右键中未复现——诊断定位为**随记宿主胶囊态/展开态的右键时序差异**（第一次右键触发 Peek→Open 展开而非弹菜单；诊断日志已加 `[QuickCaptureMenu]`），非产品缺陷（B1/B3 同构菜单机制已实测正常）。降级为代码审查 + 回归背书 + 人工复验清单 |
| **S2 布局编辑 50 次（DEF-004 剖析）** | drag 标题 ±40px 往返 ×25（50 次编辑），每 10 次分段采样 | **决定性数据**（下表）——「布局编辑直接导致 DWM 无界增长」假设排除 |

## 四、DEF-004 S2 剖析数据（原始样本 `r4-s2-samples.jsonl`）

| 编辑次数 | DeskBox Priv | Handles | GDI | DWM Priv | DWM WS |
|---|---|---|---|---|---|
| 10 | 308.5 MB | 2525 | 91 | 945.8 MB | 496.1 |
| 20 | 307.7 | 2580 | 91 | 958.6 | 506.6 |
| 30 | 308.8 | 2583 | 91 | 955.7 | 503.3 |
| 40 | 311.7 | 2592 | 91 | 955.5 | 503.1 |
| 50 | 313.5 | 2587 | 91 | 955.5 | 503.0 |

**结论**：① DeskBox Private +5MB 后平台化（非无界泄漏）；② Handles 初始爬升后稳定；③ **GDI 恒定 91（零泄漏）**；④ **DWM Private +10MB 后完全平台化**——单会话布局编辑的 DWM 增量有界且收敛。用户观察的 DWM 1–2GB 属**长周期累积**（多启动周期/显示拓扑变化/系统表面管理），修复方向从「编辑会话泄漏」修正为「长周期观测 + 系统协同」，ETW 长周期观测列入 R5。

## 五、审查结果（本轮改动：驱动脚本 + 诊断日志）

- 脚本：SendInput INPUT union 正确布局（keyboard/mouse FieldOffset）、DPI 感知（SetProcessDPIAware 校准物理像素坐标）、PS 5.1 兼容（无 ::new、无 PS 委托闭包）。✅
- 诊断日志 `[QuickCaptureMenu]`：每次右键一行（低频），保留作 B2 观测点。✅
- 回归：**2998/2998 全部通过**（诊断日志重建后）。✅

## 六、遗留与 R5 建议

1. **B2 人工复验**：随记展开态右键 →「记录配色」→ 自定义文字色 → 截图对比；`[QuickCaptureMenu]` 日志已就位（`clipboardColorSubItems` 计数）。
2. **DEF-004 R5**：长周期 DWM 观测（跨启动/天级的采样任务）+ ETW 表面剖析。
3. **稳定性观察期**：R3→R4 继续零返工、无新增重大问题（条件计 **2/2**）——高/中优先级缺陷中 DEF-001/002/003 已闭环、DEF-004 已定位到长周期尺度，**满足进入常态化维护的判定条件**，R5 起按固定周期执行全量扫描与小幅优化。
4. B3 图标 FileOpenPicker 人工路径与 B2 同窗口执行。

# DeskBox 全量任务 TODO 清单（长期迭代总追踪）

> 机制说明：本清单覆盖当前会话提及的**全部任务**，逐项明确分类、内容、完成标准、输出物与核验状态。
> 全部任务完成后，对照本清单**循环逐项查验**是否达标，核验结果同步更新至本文档（每次核验在「核验记录」节追加一行）。
> 本清单与核验机制统一纳入 DeskBox 长期持续优化迭代计划，作为整体任务追踪与质量闭环的标准环节。
> 创建：R2 轮中途（补充批次下达后）。核验状态取值：未开始 / 进行中 / 已完成 / 有条件完成（附条件说明）。

## A. 初始核心 Bug 修复类（长期迭代初始输入）

| 编号 | 任务 | 对应缺陷 | 完成标准 | 输出物 | 核验状态 |
|---|---|---|---|---|---|
| A1 | 内存占用异常优化（主进程 600MB / DWM 1–2GB 关联定位） | DEF-004 | 源码层定位关联机制；运行时基线量化；按数据实施修复且无回归 | issues/DEF-004 文档、性能基线表更新 | 进行中（R1 完成 DWM 强关联实证：关闭长期实例后 DWM Private 5.88GB→561MB、主进程 760→318MB；GDI 泄漏假设排除（实测 200 个）；修复待基线驱动） |
| A2 | 悬停自动展开动画卡顿与失效修复 | DEF-002 / DEF-003 | ToolTip 阻塞失效根除；动画逐帧开销收敛至稳定 ≥60fps | issues/DEF-002、DEF-003 文档 | 有条件完成（DEF-002 已修复+回归 2998/2998；DEF-003 已消除每帧分配，Win11 合成器动画候选列 R2，60fps 未实测） |
| A3 | 显示桌面后部分格子不显示 | DEF-001 | 自愈机制覆盖最小化与 cloak 两种 shell 行为；目标机场景复现通过 | issues/DEF-001 文档、[ShowDesktop] 日志证据 | 有条件完成（R1 修复 + R2 中间态修正事件常量 bug：MINIMIZESTART=0x0016、补 FOREGROUND 事件源、dispatcher 线程注册；**待实测确认**——独立监听器已证实 FOREGROUND 事件可达，应用内全链路复验因批次切换暂缓） |

## B. 新增功能类（补充批次）

| 编号 | 任务 | 完成标准 | 输出物 | 核验状态 |
|---|---|---|---|---|
| B1 | 格子边距手动精确输入（统一/分边、实时预览、0–200px 拦截容错、与拖动双向同步、随布局持久化、批量设置） | 功能代码完成；代码层面 5 维审查通过；兼容现有布局保存/加载/导出 | FEATURE-B1 文档 + 代码 | 有条件完成（代码+审查+回归 2998/2998；GUI 实测待下一运行窗口） |
| B2 | 随记剪贴板记录自定义配色（文字/背景色、取色器+HEX、跟随主题默认、自定义优先、对比度校验、一键恢复、持久化） | 同上 | FEATURE-B2 文档 + 代码 | 有条件完成（同上） |
| B3 | 格子标题布局自定义、图标自定义与格子组标题排序（左/中/右对齐、本地图标 PNG/ICO/JPG+裁剪适配、组内拖拽排序、批量应用、持久化） | 同上 | FEATURE-B3 文档 + 代码 | 有条件完成（对齐/图标/批量已实现；组排序为既有能力确认——菜单上移/下移+MemberIds 持久化已存在，标签拖拽列为后续增强；GUI 实测待补） |

## C. 专项 Bug 修复类（补充批次）

| 编号 | 任务 | 完成标准 | 输出物 | 核验状态 |
|---|---|---|---|---|
| C1 | 点击格子标题区域时其他格子闪烁修复 | 根因源码定位；最小侵入修复；不破坏标题点击选中/展开/收起交互；无额外性能开销 | BUG-C1 文档 + 代码 | 有条件完成（根因=全员瞬态置顶带迁移；修复=专用合批方法；回归 2998/2998；GUI 实测待下一运行窗口） |

## D. 质量保障类

| 编号 | 任务 | 完成标准 | 输出物 | 核验状态 |
|---|---|---|---|---|
| D1 | 补充批次全量代码审查（无实际运行测试）：逻辑正确性/异常处理/资源管理/性能影响/一致性 | 逐项审查记录在文档；x64 自动化回归全绿（自动化测试属代码级校验，非 GUI 运行测试） | 各文档「代码审查要点与逻辑验证结论」节 | 已完成（五维结论记录于各子文档第五节；回归 2998/2998） |
| D2 | 长期迭代闭环机制落地：缺陷台账维护、性能基线表、轮次报告 | 台账与基线随每轮更新 | docs/quality/defect-ledger.md、performance-baseline.md、rounds/* | 进行中（R1 交付齐全；R2 中间态已提交；R2 报告与基线数字待补） |

## E. 交付物类

| 编号 | 任务 | 完成标准 | 输出物 | 核验状态 |
|---|---|---|---|---|
| E1 | 单任务独立 MD 文档（B1、B2、B3、C1 各一份，五章节模板） | 每任务一份，章节完整 | docs/quality/batch-2/FEATURE-B1/B2/B3、BUG-C1 | 已完成 |
| E2 | 总览报告（本批次改动汇总、依赖关系、审查结论、整体风险，标注为长期任务补充项） | 单独成文且引用各子文档 | docs/quality/batch-2/OVERVIEW.md | 已完成 |
| E3 | 缺陷管理台账更新（C1 立项、B1–B3 功能项入账） | 台账含全部新条目 | defect-ledger.md 更新 | 已完成（DEF-006 + BATCH2-F1/F2/F3） |
| E4 | 本清单建立 + 全部任务完成后循环核验并更新核验状态 | 清单覆盖全部任务；终核验逐项打勾并留核验记录 | 本文档 | 进行中（清单已建立；终核验待全部任务完成后执行） |

## F. R6 全量并行代码缺陷深度审查（周期性全量扫描 · 本轮批次）

| 编号 | 任务 | 完成标准 | 输出物 | 核验状态 |
|---|---|---|---|---|
| F1 | 10 个专项 subagent 并行审查（S1 内存资源 / S2 动画渲染 / S3 窗口交互 / S4 格子布局 / S5 随记功能 / S6 事件消息 / S7 配置持久化 / S8 线程安全 / S9 异常处理 / S10 架构兼容），纯静态、不运行测试 | 每专项独立报告，问题含位置（file:line）/触发条件/风险等级/影响/根因/证据；复核已知问题修复有效性 | rounds/round-06/S01~S10（10 份，2,214 行） | 已完成（RPM 限流致多次失败，经错峰重试/断点续跑全部交付；每报告含覆盖率声明） |
| F2 | 主流程统一汇总复核：去重合并、分级分类、对当前树二次核验 | 总报告含问题总览/风险分布/优先级排序/整改建议；**审查期间代码树变动（15:02 提交 8ea60ee + 未提交修改）逐条定性**——3 条立案时成立、汇总时已被整改顺手修复（LAY-02/CFG-02/CFG-06），1 条跨专项合并（LAY-01≡CFG-01、EVT-03≡EXC-03），净立案 67 条（P1×3、P2×19、P3×45） | rounds/round-06/全量代码缺陷审查总报告.md | 已完成（含 §2 审后复核修正节与逐条核验证据） |
| F3 | 缺陷台账入账 | DEF-008~027（20 条：P1×3、P2×17）逐条立案；P3 45 条（另 QC-04 维持 S05 的 P2 定级、位于死宿主随 DEF-027 处置）以 R6 卫生批次清单入账；R1 观察清单三项复核结论更新（ARC-06 确认成立、CS0169 改判归属 ANI-02 死代码链路、OnboardingWindow 字段仍开放） | defect-ledger.md | 已完成 |
| F4 | 本清单新增 F 节 + 核验记录 | 覆盖本轮全部任务并留核验记录 | 本文档 | 已完成 |
| F5 | P1 修复（DEF-010 跨线程刷新主题 → DEF-008 批量边距锚点+钳制 → DEF-009 随记自写标记） | 最小侵入修复；x64 回归全绿；补列用例（启动主题刷新、批量边距→重启位置保持、随记复制无自回录）；DEF-009 修复时一并核验 SearchPopupWindow 三处剪贴板写入 | 代码 + 下轮报告 | **已完成（R6-P1 整改批次）**：报告拆解核对 + 三修复专项并行闭环 + 交叉验证三项 PASS + 总复核 GO；构建 0 错误、x64 回归 3006/3006（+8 新用例）；SearchPopup 三处证实为同型遗漏并全部修复；独立核对文档 `rectify/R6-P1-triage.md`、整改报告 `rectify/R6-P1-remediation.md` |
| F6 | P2 四批次修复（A 窗口 DEF-014~017 / B 随记 DEF-011~013 / C 稳定性 DEF-018~026 / D 架构 DEF-027） | 同上；DEF-027 删死宿主前需 GUI 回归随记全功能并修订 current_architecture.md 与 [重要勿删] 手册（含 WIN-01/05/06 文档矛盾消除）；修 [重要勿删] 相关项不得触碰 z-order 核心约定 | 代码 + 文档 |  **已完成**（A=`d2e7e87` DEF-014/015/017；B=`f25cb77` DEF-011/012/013；C=`12dbbf2` DEF-018~026 含 DEF-022；D=`ad8febe` DEF-027/016；报告 rectify/F6-*.md；收敛式深度审查第 1 轮零新增 P0/P1/P2 判收敛，见 rectify/final-review/） |
| F7 | P3 卫生批次（45 条按域归并） | 按域顺手修复；ARC-06/ANI-02/CFG-07/QC-08/EVT-04 五项死代码同批删除；吞异常补注释/日志 | 代码 | **已完成（`4682a02` + 前序批次覆盖）**：ARC-06 删除（含测试）、ANI-02 全链删除（约 1,000 行）、CFG-08 补 Common.Close/More ×12 语言（2561 一致）、CFG-07/EVT-04 复核为已接线不删（注释锚点）、QC-04/08/15 与 EVT-03/EXC-03 及 WIN-05/06/07 等随前序批次消除；其余 P3 逐条归类挂账（见 rectify/F7-hygiene-batch-remediation.md 与第 14 次核验行） |

## 核验记录

| 日期 | 核验轮 | 结论 |
|---|---|---|
| （R2 中途） | 清单建立 | A1/A3/D2/E4 进行中；A2 有条件完成；B/C/D1/E1–E3 未开始；等待补充批次实施后统一循环核验 |
| （补充批次实施后·第 1 次循环核验） | B1/B2/B3/C1 | C1 已修复（回归 2998/2998，GUI 实测待下一运行窗口）；B1/B2/B3 代码完成（构建 0 错误、回归 2998/2998、12 语言同键校验通过，GUI 实测按批次约束待补）；D1 五维审查已随实现完成并记录于各子文档第五节；E1 四份独立文档 + OVERVIEW 已产出；E3 台账已更新；A1/A3/D2/E4 状态维持（A3 自愈修复已含 R2 中间态修正，A1 基线采集待目标机运行窗口）；**遗留**：GUI 场景实测统一列入下一运行窗口，届时再核验升级状态 |
| （R2 收尾·第 2 次循环核验） | A1/A2/A3/D2 | A3：DEF-001 自愈链路三要素目标机实测通过（双钩子注册/Win+D 核验/真实最小化核验，verbose 留痕），状态升级为「有条件完成（正向恢复用例留生产日志观察）」；A2：DEF-003 候选 1 完成（Win11 合成器动画启用，契约更新，回归 2998/2998），60fps 实测 dropped 对比待人工复验清单；A1：DEF-004 关联实测坐实并落基线表，修复待 R3 表面剖析数据；D2：R2 报告/变更记录/基线表已产出并提交。**整体**：GUI 自动化工具在目标机对 owner-attached 格子窗口受限，悬停/布局编辑类场景统一列入人工复验清单（各文档已附用例） |
| （R3 收尾·第 3 次循环核验） | 人工复验清单 | DEF-001 正向恢复用例端到端通过（人为 iconic → Restored restored=1，窗口数 12）；DEF-002 悬停实测正常响应；候选 1 dropped 对比 **3/611（0.49%）vs 修复前 3–10%**，60fps 红线实测达成，视觉回归通过；候选 3 决议不实施（服务未接线+稳态已达标，列 R4）；DEF-004 剖析列 R4（R3 指令二选一已完成候选 3 调查决议）。**稳定性观察期开始计算**：R2→R3 零返工、无新增重大问题（连续两轮条件计 1/2）。A1 修复与剩余 GUI 实测（B 批次）列 R4 运行窗口 |
| （R3 收尾·第 3 次循环核验） | 人工复验清单 | DEF-001 正向恢复用例端到端通过（人为 iconic → Restored restored=1，窗口数 12）；DEF-002 悬停实测正常响应；候选 1 dropped 对比初测 3/611（0.49%）vs 修复前 3–10%（**R4 更正：该数据来自候选 1 之前二进制，候选 1 真 A/B 在 R4 补测，见第 4 轮**）；候选 3 决议不实施（服务未接线+稳态已达标）；DEF-004 剖析列 R4。稳定性观察期开始计算（1/2） |
| （R4 收尾·第 4 次循环核验） | B 批次 GUI 实测 + DEF-004 剖析 | **C1 像素级闭环**（点击标题前后其他格子区域 changed=0/maxDelta=0）；**B1 全链闭环**（对话框→输入 66 即时移动→取消精确恢复，diff 29.44% 为恢复位移证据）；**B3 对齐闭环**（居中实测+恢复左对齐；图标 picker 人工清单）；**B2 降级声明**（菜单复验受随记胶囊态右键时序影响，诊断日志已就位，代码审查+回归背书）；**DEF-004 定位完成**（S2 50 次编辑分段采样：单会话增量有界平台化、GDI 零泄漏、DWM 增量 +10MB 后持平——「编辑会话泄漏」排除，R5 转长周期观测）；**候选 1 真 A/B**（R3 数据更正为 R1 版效果；R4 重测候选 1 dropped=0，三组数据链完整）。**稳定性观察期 2/2 达成**：R3→R4 零返工无新增重大问题——**满足常态化维护判定条件，R5 起转入周期性全量扫描与小幅优化** |
| （R4 收尾·第 4 次循环核验） | B 批次 GUI 实测 + DEF-004 剖析 | **C1 像素级闭环**（点击标题前后其他格子区域 changed=0/maxDelta=0）；**B1 全链闭环**（对话框→输入 66 即时移动→取消精确恢复，diff 29.44% 为恢复位移证据）；**B3 对齐闭环**（居中实测+恢复左对齐；图标 picker 人工清单）；**B2 降级声明**（菜单复验受随记胶囊态右键时序影响，诊断日志已就位，代码审查+回归背书）；**DEF-004 定位完成**（S2 50 次编辑分段采样：单会话增量有界平台化、GDI 零泄漏、DWM 增量 +10MB 后持平——「编辑会话泄漏」排除，R5 转长周期观测）；**候选 1 真 A/B**（R3 数据更正为 R1 版效果；R4 重测候选 1 dropped=0，三组数据链完整）。**稳定性观察期 2/2 达成**：R3→R4 零返工无新增重大问题——**满足常态化维护判定条件，R5 起转入周期性全量扫描与小幅优化** |
| （R5 收尾·第 5 次循环核验） | 阶段总览 + DEF-004 观测启动 + B2/B3 复验环境受阻 | **阶段总览报告已补交**（`stage-review-r1-r4.md`：R1–R4 收敛度 P0 1/1、P1 3/4、P2/P3 各 1/1；性能三组 A/B 数据链；稳定性评估含 R3 二进制时间戳教训；维护建议五条）；**DEF-004 长周期观测机制落地**（`record-longperiod-sample.ps1` 自动发现实例采样 + 每 6 小时定时任务 automation-ef5dd1b6 + `r5-longperiod-samples.jsonl`，含实例缺席标记），数据积累 3–7 天后按增长形态定论；**B2/B3 复验环境受阻**：目标机已转用户活跃使用（Chrome 前台），GUI 自动化继续执行会干扰用户——两项维持人工复验清单（诊断日志与操作脚本均已就位，运行窗口执行）。台账已同步。**R5 交付**：阶段总览 + 观测机制 + 脚本改进（DPI 感知、RightTap 按住间隔） |
| （R5 收尾·第 5 次循环核验） | 阶段总览 + DEF-004 观测启动 + B2/B3 复验环境受阻 | **阶段总览报告已补交**（`stage-review-r1-r4.md`：R1–R4 收敛度 P0 1/1、P1 3/4、P2/P3 各 1/1；性能三组 A/B 数据链；稳定性评估含 R3 二进制时间戳教训；维护建议五条）；**DEF-004 长周期观测机制落地**（`record-longperiod-sample.ps1` 自动发现实例采样 + 每 6 小时定时任务 automation-ef5dd1b6 + `r5-longperiod-samples.jsonl`，含实例缺席标记），数据积累 3–7 天后按增长形态定论；**B2/B3 复验环境受阻**：目标机已转用户活跃使用（Chrome 前台），GUI 自动化继续执行会干扰用户——两项维持人工复验清单（诊断日志与操作脚本均已就位，运行窗口执行）。台账已同步。**R5 交付**：阶段总览 + 观测机制 + 脚本改进（DPI 感知、RightTap 按住间隔） |
| （复测整改收尾·第 6 次循环核验） | 五项复测结论整改 | **事实基线**：①复测五项结论与仓库 HEAD 证据系统性冲突，唯一自洽解释为复测跑了 `D:\DeskBox` 安装版 1.4.8.0（构建于 2026-08-29 19:14，早于全部五轮工作，实测存在闪烁/无边距配色功能/内存动画为原始表现）；②整改要求中的「Vue3 前端」不存在于本仓库（两个子代理独立核验：零 Vue/前端文件，技术栈 WinUI 3/XAML + C# + Rust）——已作为根因记录并落地 `[Build]` 启动身份日志（实测生效）根治识别问题。**真整改（并行三子代理审计 → 主流程复核后实施）**：③B2 复测结论**部分成立**——功能实现挂在零实例化死窗口 `QuickCaptureWidgetWindow`（审计 A 以 95% 置信度证实不可达）；已完整移植生产宿主 `QuickCaptureSurfaceContent`（XAML 画刷/应用逻辑/取色器/持久化）+ `ContentWidgetWindow` 菜单入口 + NormalizeOverrides 入管线（RECT-3）；④B1 六处真实缺口全部修复（位置锁定绕过/胶囊几何持久化/程序化回写虚假校验/远侧编辑被吞/Save 静默失败/uniform 陈旧值，RECT-4）；⑤新立 DEF-007 并闭环：StackPopover 关闭按钮交互深度泄漏封锁全部空闲内存回收（RECT-1——正是复测「内存居高不下」的一个确定性成因）；⑥DEF-003 冷启动首展 warm-up 两处增强（门控外预算化预热 + epoch 重挂 urgent，RECT-2）；⑦DEF-006 补强（peer idle 序 + 兜底锚定，RECT-5）；⑧DEF-004 长周期观测维持运转（12:08 首份样本已采）。**门禁**：构建 0 错误、x64 回归 2998/2998、五份整改版文档（rectify/RECT-1~5）产出、台账与功能台账同步。**结论**：五项复测项全部按整改要求闭环（两项证明二进制陈旧并固化识别机制、一项真缺陷移植、一项六缺口修复、一项像素级成立+补强） |
| （R6 收尾·第 7 次循环核验） | 十专项全量并行审查（F1~F4） | **F1**：10 份专项报告全部交付（S01~S10，共 2,214 行；执行中受 RPM 限流多次失败，经错峰重试/断点续跑补齐；每报告含覆盖率声明与逐条代码证据）。**F2**：总报告产出，核心增值为审后对当前树的二次核验——审查执行与 15:02 提交 `8ea60ee`（复测整改批次）及未提交修改并行，时间窗交叉导致 3 条发现（LAY-02/CFG-02/CFG-06）在汇总时已被顺手修复（逐条给出当前树证据），1 组跨专项重复合并（LAY-01≡CFG-01；EVT-03≡EXC-03），**净立案 67 条：P0×0、P1×3（DEF-008 批量边距锚点回退、DEF-009 随记自写回录、DEF-010 启动跨线程刷主题）、P2×19、P3×45（含 2 条死宿主内条目）**。**F3**：台账已入账（DEF-008~027 共 20 条 + R6 卫生批次节 + 观察清单三项复核更新）。**正面复核链**：DEF-001/002/003/006/007 修复健壮性经多专项交叉复核全部成立；Rust 冻结契约（ABI 2/十导出/16 结构体/panic 边界）零漂移；AOT/Win10 兼容面无守卫缺口；Strings 12×2,545 键一致；数据丢失防护闭环；DEF-004 维持观测中（托管侧无匹配量级候选源）。**已知修正**：S04/S07 部分结论因审查时间窗早于整改提交而被修正（总报告 §2 留痕）；「Vue3 前端」差异再次独立核验并记录（与第 6 次核验结论一致）。**F5~F7**：未开始，作为下一轮迭代输入（建议顺序见总报告 §7） |
| （遗留清单批次收尾·第 8 次循环核验） | ①动画帧率四档 ②随记配色修复+入口迁移 ③标题点击闪烁 | **①帧率四档已实现**：常规选项卡动画组新增「动画帧率」四档（30/60/90/120，int SettingsOption + SelectedValue 双向绑定），`ResolveSkipForFrameRate` 按刷新率/目标取整恒≤目标（165Hz：120→≈82fps/90→82/60→55/30→27），cap 激活时旁路自适应降级与 Escalate（用户选定即最终节奏），结束帧不受 cap 限制保证终态精确；两处 warm-up 增强（门控拒绝分支预算化预热切片 + epoch 重挂可见窗口 urgent）针对冷启动首展 dropped=31/60；AOT 契约三处同步（本地化控件计数 333、bindable 清单 +2 项、nameof 308）。**②随记配色入口已迁入「功能格子-随记」选项卡**：`QuickCaptureSettingsSection` 新增「记录配色」Expander（文字色按钮显示跟随主题/自定义状态、背景色按钮显示当前 HEX、恢复默认），共享编辑器 `QuickCaptureClipboardColorEditor` 为格子菜单与设置入口的单一事实源（对比度校验/持久化/恢复默认不漂移）；**「无法生效」两残余根因修复**：生产模板时间戳显式主题前景绕过容器继承（新增派生次级画刷 0xE8 降阶并改引）、TryGetThemeColor 缺 Brush 形态回退；提交经 `ApplyQuickCaptureClipboardColorsToLoadedWidgets` 即时推送所有已加载 surface。**③闪烁结构修复**：定位残余机制为「丙烯酸背景格子任何 z 序移动都改变其身后内容→整窗重采样闪烁」（R4 局部采样区域恰未覆盖此效应）；修复为标题点击**只抬升被点击格子**（handles=[active]，peers 零 SetWindowPos/零 owner 变更=结构零闪烁），标题 flyout 由被点击窗口拥有不受影响，整组唤起仍走 F7/托盘功能语义，2300ms 兜底仅恢复 active。**门禁**：构建 0 错误、x64 回归 2998/2998（AOT 契约同步后全绿）。三份 VERIFY 文档（rounds/round-07/VERIFY-1~3）含人工复验清单；运行实例已更新至本批构建（[Build] buildTime=2026-08-30 16:54:29）供用户人工测试。与第 7 次核验（R6 十专项）衔接：其 P1 立项 DEF-008~010 列入下一轮 |
| （R6-P1 整改收尾·第 9 次循环核验） | F5：三项 P1 专项整改（DEF-008/009/010） | **流程**：报告拆解核对子代理产出独立参照文档 `rectify/R6-P1-triage.md`（三项全部 CONFIRMED，行号漂移重新定位；SearchPopup 三处定性为需修；发现报告未点名的新位点 FileSurfaceContent.xaml.cs:3745 回退分支与 2 个无防护订阅者，列观察项）→ 三个修复专项 subagent 并行（文件集互不相交，各自「核对→修复→自验证」闭环）→ 主流程统一门禁（停止仓库实例 → Debug x64 构建 0 错误 → x64 回归 3006/3006，含 +8 新用例）→ 交叉验证子代理逐项一票驳回权验证（**三项全 PASS**，重点亲证 SP-1 的 MarkWrite 位于 await SetDragPayloadAsync 之后无隔窗失效面、manager 层 CaptureAnchor ≡ 窗口层等价性、启动期广播零订阅者）→ 总复核子代理跨模块统一复核（**GO**：三域状态空间互不相交、启动恢复只读锚点无耦合、批量三连调零重入/O(n²) 无回退、z-order 与 [重要勿删] 约束面零触碰、Rust 契约零漂移）。**修复内容**：①DEF-008：`MoveVisibleWidgets` 成功分支补 `CaptureAnchor`+`SynchronizeGroupLayoutFromMember`+`CaptureCurrentTopologyLayout`（与单格路径同参同语义）+ `ShiftSideToMargin`/`ShiftBoundsToNearestEdge` 落点套 `EnsureVisible` 钳制；行为用例 3（含"无锚点捕获→重启回退旧锚点 (1580,80)"机理复现）+ 源码契约用例 3（新文件 WidgetBatchMarginPositioningContractTests.cs）。②DEF-009：生产宿主 `CopySelectedQuickCaptureItemsAsync` SetContent 前补 `MarkWrite(text)`（单/批公共插入点）；SearchPopup 三处（多选复制 text+paths 双标记/单文件复制/复制路径）全部补标记；自写忽略 2 用例（异文不误伤/2.1s 过窗后恢复回录）；死宿主 2 处随 DEF-027 处置。③DEF-010：删启动 `Task.Run`+`await themeTask` 改 UI 线程同步（首刷确定性先于托盘创建/WidgetManager 构造）；`RefreshAppearance` 入口 `HasThreadAccess`+TryEnqueue 自愈重投；托盘订阅者同款防护；7 个调用点核实均 UI 线程。**非阻塞观察项**（4 条已记 triage/复核文档）：SearchPopup:3826 净警告数不变、两参 ShiftBoundsToNearestEdge 死重载建议随卫生批次删除、批量钳制 workArea 取移动前中心（保守自洽）、SP-3 缺 Flush 属既有卫生点。**遗留**：F6（P2 四批次）/F7（P3 卫生批次）未开始，为下一轮输入 |
| （独立复核整改收尾·第 10 次循环核验） | 独立复核报告整改（N1–N5） | **流程**：独立复核（3 个后台深挖 subagent + 主流程构建/回归门禁）产出 `rectify/R6-P1-independent-review.md`（三项 P1 全部**已修复**取证成立；新建 5 条缺陷 N1~N5）→ 本批按报告返工指令整改。**N1（P2）= Claude 候选 (a) 确证**：边距三条持久化路径（批量 `MoveVisibleWidgets`/单格 `ApplyOwnMarginToSide`/取消恢复）移动展开态格子时不刷新 `CompactPlacement`，折叠时胶囊回跳旧坐标且跨会话持续、组宿主 stale placement 固化传播 → 新增 `RefreshCompactPlacementAfterBoundsMove`（`WidgetWindowBase.Collapse.cs:489`，UI 线程自愈重投；展开态+已存 placement 才刷新）经 `IDesktopWidgetWindow` 暴露，三条路径在锚点捕获/持久化后调用（先修 placement 再组/拓扑同步），`BulkAppearance.cs` 误导注释改正。**N2（P2）**：SearchPopup 公开 async void 入口（ShowPopup/ShowPopupWithQuery）无异常保护 → 改经 `DispatchShowPopupAsync`（非 UI 线程先 TryEnqueue 重投）→ `ShowPopupSafelyAsync` 全管线 try/catch（Log+已可见时重聚焦）。**N3（P3）**：多选复制混合解析静默丢失败路径 → `SetDragPayloadAsync(List)` 失败路径文本不再被 `items.Count==0` 门控丢弃。**N4（P3）**：`StartDragAsync` 补 catch。**N5（P3）**：`EnsureVisible` wildly 回退非不动点约束注释 + `_trackedWindows` UI 线程归属注释。**门禁**：Debug x64 构建 0 错误（24 条既有警告）；x64 回归 **3011/3011**（3006 + 新增 5 用例 `MarginMoveCompactPlacementContractTests.cs`；既有备份测试一次偶发失败单独重跑通过、整轮重跑全绿，非本批引入）。台账新立 DEF-028（N1）/DEF-029（N2）并标记已修复。**遗留**：N3/N4/N5 为 P3 且已就地修复（N5 为注释）；F6/F7 仍待下轮 |
| （F6 批次 A 收尾·第 11 次循环核验） | F6 批次 A：DEF-014/015/017（窗口交互安全网） | **执行环境变更声明**：本批次起由 Linux 服务器纯静态方式执行（无 WinUI3 编译环境），编译/回归门禁改由 Windows 侧用户执行（`rectify/pending-windows-gate.md` 逐批汇总），Linux 侧以固化静态门禁脚本 `scripts/quality/static_gate.py` 替代（基线：12 语言 2555 键一致 / async void 263 / 剪贴板 12 写全配对 / 同步等待 134 / 空 catch 223 / 反射 7 / 契约断言重放 5191 命中 43 失联，均录 `scripts/quality/static-baseline.json`）。**修复**：①DEF-014（WIN-01）：`WidgetSessionManager.ForceResetInteractions`（清零+回退 `_stateBeforeInteraction`，对齐 EndInteraction 回落语义）+ `WidgetManager.ZOrder.cs` 看门狗（10s DispatcherQueueTimer，深度>0 且无 DeskBox 前台 → 强制清零 + `[TrayBatch] Interaction watchdog` 日志（排障手册指定标记）+ 恢复回落 + 空闲层级整理排队）；Begin 深度 0→1 装表 / End 归零停表，恢复文档承诺但源码缺失的防死锁安全网。②DEF-015（WIN-02）：Content 宿主 `ActivateRaisedFromTrayBatch` 补 `SetForegroundWindow` 返回值检查 + 失败日志（与 QuickCapture 宿主同构，坑#3 诊断锚点补齐），调用序列零变化。③DEF-017（WIN-04）：`WidgetShowDesktopSelfHealService` 新增 `IsFullyRegistered`（双钩子才算已启动，消除半注册卡死）+ 失败警告日志 + 5s 重试定时器（增量补注册）+ Dispose 对称清理。DEF-016（WIN-03）位于死宿主随 DEF-027 跳过。**验证**：Linux 静态门禁全 PASS（本批零新增 async void/同步等待/空 catch/反射/语言键，契约断言新增 12 条全命中）；新增 `WindowInteractionSafetyNetContractTests` 4 用例 + `WidgetSessionManagerTests` 3 行为用例（预期 Windows 回归 3018/3018）；独立审查 subagent 一票驳回权审查 **GO**（六项红线全过、误触发/线程时序/重入竞态/NRE 逐项核验 PASS）。**报告**：`rectify/F6-A-window-interaction-remediation.md`（含逐项根因/等价性论证/回滚方式）。**遗留**：DEF-016 随批次 D；批次 B/C/D/F7 待推进 |
| （F6 批次 B+C 收尾·第 12 次循环核验） | F6 批次 B（随记 DEF-011/012/013）+ 批次 C（稳定性 DEF-018~026） | **批次 B（`f25cb77`）**：①DEF-012 数据丢失最高优先——`QuickCaptureService` 撤销窗保留集（10s 登记自清）并入 GC 引用集，三删除出口登记/Restore 解除，既有清理语义逐字节保持，+2 行为用例；②DEF-011——三个编辑入口保记录自身格式（新建路径默认格式不变），消灭 Markdown↔PlainText 静默覆写；③DEF-013——`ApplyClipboardItemColors` 应用时刻复检有效对比度，跌破阈值背景自动回退跟随主题+日志。**批次 C（`3117668` + DEF-022 后续）**：④DEF-019 广播快照+逐处理器隔离（镜像 LocalizationService）；⑤DEF-020 启动阶段标记+双通道失败通知（4 新键 ×12 语言=2559 一致）；⑥DEF-021 双全局兜底（纯诊断+SetObserved）；⑦DEF-023 拖拽延迟供货（SetDataProvider+GetStorageItemsAsync），3 个 GetResult 与死同步入口删除、处理器保持同步；⑧DEF-024 CTS 只 Cancel 不 Dispose；⑨DEF-025 WeatherService 只读权威副本+浅拷贝三出口；⑩DEF-026 先压间距再截尾+双向居中补偿，+2 行为用例；⑪DEF-018 复核定案为死代码整体删除（live 呼吸/辉光/边框计时器均已被合成器动画取代或退役，零行为变化）；⑫DEF-022 热键钩子生命周期异步化（TryStartAsync WaitAsync 握手、钩子线程亲和保持、generation 防复活保留、同步 API 全留、UI 调用链无 ConfigureAwait(false)、AOT smoke/契约同步）。**执行插曲**：API 限流致 4 个并行修复 subagent 全部 429 中途夭折——残留半成品（含重复方法定义）已全部识别回滚，DEF-022 死前终稿经主流程逐文件串读接手完成（补 UI 亲和修复 + smoke 语法 + 契约更新）。**门禁**：Linux 静态门禁全 PASS（async void 263=基线、同步等待 134→131 净 -3、契约新增 16 条全命中、12 语言 2559×12 一致）。**报告**：`rectify/F6-BC-stability-remediation.md`。预期 Windows 回归 3025/3025（3011+14）。**遗留**：批次 D（DEF-027）与 F7 未开始 |
| （F6 批次 D 收尾·第 13 次循环核验） | F6 批次 D：DEF-027 死宿主删除 + 文档修订 | **删除**：`Views/QuickCaptureWidgetWindow*` 13 文件/7,796 行（零实例化死宿主），随其消除 QC-04、DEF-016、死宿主内 3 处剪贴板写豁免位点。**契约测试修订（18 个文件）**：所有引用死宿主源码的断言迁到生产面（`QuickCaptureSurfaceContent`/`ContentWidgetWindow`）或删除宿主专属用例——ContentDeletionDirect、QuickCaptureSettingsRuntime（含 DEF-011 新契约：编辑保自身格式）、ItemHoverAction、QuickCaptureImagePreview、MarkdownAndSplitter、FrostedActionSurface、DpiAdaptiveLayout、WidgetForeground（含 RootGrid 资源域断言迁除）、WidgetCoordinatedMove、QuickRevealLayer、WidgetCompactTrayVisibility、WidgetMoreMenuPlacement、WidgetDangerActionStyle、WidgetPresentationAndDetach、WidgetVisualActivity、SolidColorBackdrop、AotStage4E2/5B4B2B1、LocalizationResource、MultiSelectionKeyboard（Delete 键断言改指生产 surface）、SettingsCopyAndHierarchy、QuickCaptureMultiDrag、QuickCaptureMaterialRefresh。**架构文档修订**：`current_architecture.md` 4 处（provider 路由、窗口清单、QuickCapture 节、动画/布局节 users 列表）改为单宿主契约；[重要勿删] 手册 6 处（§1 宿主表、§3.3-3 `EnsureRaisedFromTrayTopMost` `_isAtDesktopLayer` 短路失效校正=WIN-05、§3.3-8/§4 唯一宿主、坑#5 四象限→两层级模式、§8-D 可观测/看门狗行现状化）；`WidgetWindowBase.cs` 类注释同步。**回归清单**：`rectify/F6-D-quickcapture-regression-checklist.md` 7 大类 30 项（Windows 侧执行）。**门禁**：静态门禁 PASS，基线随死代码消除刷新（async void 263→222、剪贴板写 12→8 全配对、空 catch 223→221、反射 7→6、契约失联 43→42）。**遗留**：F7 卫生批次、收敛式深度审查 |
| （F7 卫生批次收尾·第 14 次循环核验） | F7：P3×45 按域归并 + 死代码删除 | **实际执行**：ARC-06 `WidgetSurfaceSnapshotCache`+测试删除；ANI-02 硬件自适应动画整链删除（AdaptiveTrayAnimationController/SmartAnimationAdapter/HardwareAdaptiveAnimationService + WidgetWindowBase 静态引导，CS0169 消除）；CFG-08 补 `Common.Close`/`Common.More` ×12 语言（修对比度对话框字面量渲染 + Markdown 更多按钮回退）；Onboarding 字段观察项注释锚定。**复核改判（不删）**：CFG-07 `NormalizeGlobal` 已接线（FileWidgetFolderOpenBehaviorNames:34）、EVT-04 `RegistrationChanged` 已有 SettingsWindow 成对订阅。**前序批次覆盖**：EVT-03≡EXC-03（DEF-021）、WIN-07（DEF-029）、WIN-05/06（批次 D）、QC-04/08/15 与 QC-09（DEF-027）。**挂账**：MEM-01/02、ANI-03~06、WIN-08/09、LAY-05/06/08、QC-06/07/10/11/12/13/14、EVT-02、CFG-03/04/05/09、THR-06/07、EXC-04/05/06、ARC-02~05（逐条理由见 rectify/F7-hygiene-batch-remediation.md；QC-10 StackOverflow 面列高优先观察）。**门禁**：PASS（2561×12 语言、async void 222=基线、契约重放新增 0）。**遗留**：收敛式深度审查 |
| （收敛式深度审查收尾·第 15 次循环核验） | 收敛式深度审查（3 面并行，上限 4 轮） | **第 1 轮即收敛**：面 A（修复相邻代码面：撤销窗保留集 13 调用点/对比度回退递归/死宿主删除订阅完整性/WeatherService 浅拷贝调用方）全 PASS + 1 P3（QC-16 递归无异常保护，理论路径）；面 B（覆盖率限制区：SearchPopup 全路径精读、220 处 async void 抽检、85 处 null-forgiving、Onboarding 五分部订阅矩阵）零新增 P0/P1/P2 + 5 P3（F7-B1~B5 async void 保护不统一类）；面 C（Rust FFI 十导出零漂移、看门狗三计时器交织、异步握手 generation 防复活、DEF-020 fallback）零新增 + 2 观察项。**合计新立 5+1 条 P3 挂账**（QC-16、F7-B1~B5），无重复立案。**报告**：rectify/final-review/（convergent-deep-review-report.md + round1 三面报告）。**收敛判据达成**，不启动第 2 轮 |
| （Windows 交接验收 + DEF-030·第 16 次循环核验） | Windows 侧门禁执行（F6/F7 实机验证）+ 用户新报 DEF-030 修复 | **Windows 门禁**：`c3a5555` 本地 Debug x64 构建 0 错误（20 警告 ≤ 基线 24）；x64 回归 **3015/3015** 与 CI 一致（ReleaseEvidence 一次杀软瞬时拦截，隔离重跑通过后整轮全绿）；实机 PID + `[Build] buildTime` 身份核实；日志锚点核验：`Self-heal watcher started` 双钩子非 0（DEF-017 ✓）、全日志零 `Interaction watchdog`/`SetForegroundWindow FAILED`（DEF-014/015 安全网静默 ✓）；DEF-020 注入验证通过（`Exception in OnLaunched phase=widget restoration` → `[Startup] Failure notification: title=DeskBox 未能完全启动 phase=格子恢复` 本地化正确、通知达系统通知中心（LastNotificationAddedTime 更新）、进程存活；横幅视觉被系统勿扰抑制属 OBS-C-2 已知 design trade-off）；注入代码精确还原（工作树 diff 校验）。随记回归 A1 ✓（剪贴板文本→列表顶部 PlainText）。**用户新报 DEF-030（P2）**：随记记录悬停主文字变白与浅色卡片融合且不可配置——根因为系统 ListViewItem 模板 PointerOver 前景仅随面板主题（主文字行无显式 Foreground 被模板接管）；新增 `QuickCaptureClipboardItemHoverForegroundBrush` 第三配色通道：跟随主题默认经 `ResolveAutoHoverTextColor` 按有效卡片背景自动黑白高对比（根因消除），自定义走既有编辑器同对比度校验，悬停进出显式设色/ClearValue 还原 + 虚拟化复用路径清残留，组件菜单 + 设置页双入口，+4 键 ×12 语言（2569 全一致），冻结控件集基线 166→167/136→137/333→335 随新增本地化 SettingsCard 上调。**门禁**：构建 0 错误；回归 **3024/3024**（+9 新用例 `QuickCaptureHoverTextContrastTests`）；实机验证：深色卡片悬停白字可读（浅色卡场景由数学用例锁定），GUI 自动化因用户占用机器提前收口。**遗留**：随记 30 项清单余项（B/C/D/E/F 组）与 PR #1 合并决策待机器空闲后继续 |
| （F8 R2 Round 1·第 17 次循环核验） | Linux 接手：上游 1.4.9 基线全量审查 + 缺陷修复 + CI 闭环 | **基线**：191c93e（上游 1.4.9 内存优化 0ea2ddb + 我方闪烁修复链）。**审查**：3 个并行独立 subagent（面A 相邻代码面 `/root/review-findings.md`：1×P1+2×P2+2×P3；面B 覆盖率限制区+台账复核 `round-06/independent-review-round06-coverage-limit.md`：零新增 P0/P1/P2，挂账逐条复核；面C 契约+线程面：Rust 冻结契约十导出零漂移、上游 async void +7 逐条定性、empty catch +6 全部显式 fallback 合规）。**修复 4 项**：DEF-034（P1）双 lock 间二次短路检查；DEF-035（P2）StoreStartupService 缓存+PrefetchTaskAsync；DEF-036（P2）RefreshStartupToggleFromSystem 改 TryEnqueue(Low)；DEF-037（P2）两处 replaced CTS 补 finally Dispose。**CI 收敛 4 轮**：663c593/cf0fadb/3f2504d 三连败（根因 CS0104——OnboardingWindow.Hotkey.cs 同时 using Microsoft.UI.Dispatching 与 Windows.System，裸 DispatcherQueuePriority 二义性）→ 4701b39 全限定消除 → **run 33561624305 Build+Test 全绿**。**基线漂移**：async void 222→229 / sync_wait 131→132 / empty catch 219→225（全部来自上游合并，static-baseline.json 已注明理由更新）。**SimplifyBackdrop P2 复核为误报不立案**（LegacyAccentBackdropActive=true 蕴含 UsesLegacyWindowAcrylic=true，不变量成立）。**门禁**：静态门禁 PASS（契约 5353 命中/42 失联新增 0、12 语言 2582×12 一致）。**独立审查**：修复批次 subagent 一票驳回权审查 **GO**（4/4 文件无 P0/P1；关键定论 StartupTask.State 为活属性、缓存语义正确）；2×P2（延迟刷新回弹竞态→代际护栏 / disposed CTS 入口 ODE 绕过遥测→Token 移入 try+专项 catch）与 3×P3 注释更正全部同批闭环（bb708e3，run bb708e3c 全绿）。**Round 2 收敛审查**（deleg_8d8b2d7b）：②③④ 全 GO；① 护栏 NO-GO——用户拨动不递增代际，在途回调仍可回弹（立案 DEF-038）→ 917bcf9 一行修复，run 917bcf9e 全绿。**R2 判定收敛**（第 2 轮，无新增 P0/P1/P2 未处置项；两条新 P3 挂账）。**报告**：rectify/F8-linux-hermes-review-round1.md + F8-round1-fix-report.md。**遗留**：DEF-034~037 实机验证（pending-windows-gate F8-1~4）、面A 两条 P3 挂账（HWND_TOP sentinel/重复 lock 语义） |

## 第 18 次循环核验（F8 R3+R4，2026-09-02）
- R3：主线亲审（3 subagent 全 429 阵亡后接管），新立案 DEF-039（P2：es-ES/fr-FR/ru-RU 非法日期格式字母 → 时间戳假字，1.3.8 起潜伏），修复 41d70e7 + 新契约测试锁死格式字母白名单，CI 绿（run 4c16e05d）。
- R4：**收敛判定达成**。主线亲审 9 面（BoundedStaOperationRunner/OpenItem/FileMetaService/ShortcutHelper/IconBitmapQuality/MemoryCleanupPolicy/PerformanceLogger/占位符 arity/RTL）全 GO 零新缺陷；subagent（deleg_96eac299）FileSurfaceContent 大面 GO（五项重点核查全过），挂账 P3×3（DEF-040/041/042）。
- 终态：HEAD=4c16e05+docs 增量，CI 全绿，4 轮完成（硬上限 5），净减缺陷：P1×1 + P2×4（DEF-034/035/036/037/038/039）。

# DeskBox 1.5.3

DeskBox 1.5.3 improves startup reliability and notification activation. Direct installations gain a standard startup method with an optional scheduled task, and both distribution channels register notifications before decoding activation arguments.

Release date: 2026-09-16 (prepared from the 2026-09-16 `fix/autostart-boot-reliability` working tree).

## Highlights

- **Choose a startup method on direct installations.** New installations default to the standard Windows Run entry. Existing installations retain their active method. Scheduled tasks remain optional; a failed task registration falls back only after the standard entry is verified and alternative entries are cleaned up. Failed switches preserve the previous registration, Windows opt-outs are respected, and choosing a method while startup is off does not enable it.
- **Preserve Unicode task paths.** Task definitions and rollback snapshots are read directly as Unicode through Task Scheduler COM. Chinese account names and installation paths no longer depend on guessing a redirected console code page.
- **Fix notification activation initialization.** The notification manager is registered before rich activation arguments are read. Unsupported or failed registration skips notification decoding while ordinary startup remains available. Early notifications use the durable activation queue until the main instance is ready. The Store version keeps its Windows StartupTask integration.
- **Recover from early startup failures.** Tray creation retries while the desktop shell starts, and fatal initialization failures terminate instead of leaving a headless process holding the instance lock. The startup watchdog also covers constructor initialization. Failed first-time autostart registration remains eligible for a later retry.
- **Keep diagnostic checks readable.** Boolean verification results such as path matching and execution-limit checks survive diagnostics redaction, while actual paths and account identifiers remain hidden.

See `CHANGELOG.md` for the complete list.

## Downloads and verification

### GitHub Releases installers

| File | Architecture | Size | SHA-256 |
| --- | --- | ---: | --- |
| `DeskBox_Setup_1.5.3_x64.exe` | x64 | 41.96 MiB | `A4B879E683C30515288F24B956CE84EB1AEC62AE86BA4A4176483C407EC5D8D1` |
| `DeskBox_Setup_1.5.3_arm64.exe` | ARM64 | 41.45 MiB | `7A007B7FC94AD5DDFE8F8CC12C3D5AD7F2BB32AFD0D1C3BC0C552043E95AC248` |

The direct installers are Native AOT builds with the matching private Windows App Runtime bundled for offline installation. They are not Authenticode-signed. Matching `.sha256` sidecars accompany both installers.

### Microsoft Store upload

| File | Architectures | Size | SHA-256 |
| --- | --- | ---: | --- |
| `DeskBox_1.5.3.0_x64_arm64.msixupload` | x64 + ARM64 | 71.25 MiB | `8ACE2F5C6AF18364CAF341D090E10D61921EC62997134B54554311E8C4F00AA9` |

Submit the combined `.msixupload` to Partner Center after approval. It contains `DeskBox_1.5.3.0_x64_arm64.msixbundle` and the matching x64 and ARM64 `.appxsym` files. The internal bundle has version `1.5.3.0` and SHA-256 `A51C31A248ACB87EDA89477C31BB2F438371810B104901639153802D3AE07DB8`.

The single-architecture `.msixupload` outputs are intermediate build evidence, not the final submission artifact.

Source baseline: `53d8441bc37a583ea4fb35c26f3f6a8de9239be6`, plus the startup-method, Unicode task query, notification activation, diagnostic and documentation changes finalized in this release commit on `fix/autostart-boot-reliability`. Pushing, tagging and publication happen after these commits.

## Validation

- x64 regression suite: 3582/3582 passed on 2026-09-16.
- Debug build: passed, 0 errors; one canonical hotfix instance at ordinary-user integrity.
- Unicode task integration probe: the production UTF-8 manifest, STA/MTA callers, Unicode paths, snapshot restoration and Native AOT were verified on the local Windows 11 machine.
- Independent Native AOT MSIX probe: COM cold activation, a callback to the same running process, and cross-process envelope forwarding passed; `snooze=30m` was preserved. The temporary package was removed. This is not a Notification Center human-click test or a Store flight.
- Direct x64/ARM64 Native AOT payload and installer checks: passed for x64 and ARM64; source remained unchanged during both publishes.
- Combined Store container and both architecture audits: passed; bundle identity/version, architecture coverage, symbol files and inner-package hashes verified.

## Remaining release-boundary checks

- Windows 10 login checks passed on 2026-09-16 (standard and scheduled-task startup, method switching, tray visibility at ordinary-user integrity); repeat the login checks on Windows 11 before publishing.
- Pushing, the GitHub release and the Partner Center submission await explicit approval.
- Real notification clicks with the application stopped and running, including notification buttons and selection input, on the final Store package.
- ARM64 device execution, Store flight upgrade and WACK remain separate checks; cross-compilation and package audits do not establish them.
- Set the actual release date and verify README download links after publication is authorized.

## 中文说明

DeskBox 1.5.3 改善开机自启与通知激活的可靠性。直装版增加标准自启和可选的计划任务，两种发行渠道都会先注册通知，再读取通知激活参数。

2026 年 9 月 16 日发布（产自 2026-09-16 的 `fix/autostart-boot-reliability` 工作区）。

### 主要更新

- **直装版可选择自启方式。** 新安装默认使用标准 Windows Run 启动项，已有安装保留实际生效的方式。计划任务作为可选项；注册失败时，只有标准入口验证和替代入口清理成功才报告回退成功。切换失败保留旧注册，尊重 Windows 禁用状态，关闭自启时改选方式只保存偏好。
- **完整读取 Unicode 任务路径。** 计划任务定义与回滚快照直接通过 Task Scheduler COM 读取，不再猜测重定向输出的代码页，避免中文账户名和安装路径在校验时被读错。
- **修复通知激活初始化顺序。** 先注册通知管理器，再读取激活参数；不支持通知或注册失败时跳过通知解码，保留普通启动。初始化期间的通知先进入持久队列，等主实例就绪后处理。商店版继续使用 Windows StartupTask。
- **改善登录早期的启动恢复。** 桌面外壳启动期间重试托盘创建；不可恢复的初始化失败会退出，避免留下占用实例锁的无界面进程。看门狗也覆盖构造阶段，首次自动开启自启失败后仍可在后续启动重试。
- **保留有用的诊断检查结果。** 路径匹配、执行时限等布尔检查结果不再被误脱敏，实际路径与账户信息仍会隐藏。

完整条目见 `CHANGELOG.md`。

### 下载与校验

#### GitHub Releases 安装包

| 文件 | 架构 | 大小 | SHA-256 |
| --- | --- | ---: | --- |
| `DeskBox_Setup_1.5.3_x64.exe` | x64 | 41.96 MiB | `A4B879E683C30515288F24B956CE84EB1AEC62AE86BA4A4176483C407EC5D8D1` |
| `DeskBox_Setup_1.5.3_arm64.exe` | ARM64 | 41.45 MiB | `7A007B7FC94AD5DDFE8F8CC12C3D5AD7F2BB32AFD0D1C3BC0C552043E95AC248` |

两个直接安装包均为 Native AOT 构建，内置匹配架构的私有 Windows App Runtime，可离线安装。安装包未进行 Authenticode 签名，各附带一个 `.sha256` 校验文件。

#### Microsoft Store 上传包

| 文件 | 架构 | 大小 | SHA-256 |
| --- | --- | ---: | --- |
| `DeskBox_1.5.3.0_x64_arm64.msixupload` | x64 和 ARM64 | 71.25 MiB | `8ACE2F5C6AF18364CAF341D090E10D61921EC62997134B54554311E8C4F00AA9` |

确认后向 Partner Center 提交合并的 `.msixupload`。其中包含 `DeskBox_1.5.3.0_x64_arm64.msixbundle` 和匹配的 x64、ARM64 `.appxsym`。内部 bundle 版本为 `1.5.3.0`，SHA-256 为 `A51C31A248ACB87EDA89477C31BB2F438371810B104901639153802D3AE07DB8`。

分架构 `.msixupload` 仅用于构建取证，不能替代最终合并上传包。

源码基线为 `53d8441bc37a583ea4fb35c26f3f6a8de9239be6`，加上本发布提交定稿的自启方式、Unicode 查询、通知激活、诊断和文档修改（分支 `fix/autostart-boot-reliability`）。推送、打标签与发布在这两笔提交之后进行。

### 验证状态

- 2026-09-16 x64 全量测试 3582/3582 通过。
- Debug 构建：通过，0 个错误；仅运行一个规范路径的 hotfix 普通用户实例。
- 本机 Windows 11 的 Unicode 任务探针通过了原应用 UTF-8 清单、STA/MTA 线程、中文路径、快照恢复和 Native AOT 验证。
- 独立 Native AOT MSIX 探针通过 COM 冷启动、同一进程运行中回调和跨进程参数转发，保留 `snooze=30m`，临时包已卸载。该验证不等于通知中心真人点击或真实商店 flight。
- x64/ARM64 直接安装包及 Native AOT 载荷：双架构均通过，两次发布编译期间源码保持不变。
- 合并商店容器及双架构审计：通过，已核对身份、版本、双架构覆盖、符号文件及内部包哈希。

### 待完成的发布边界验证

- Windows 10 登录检查已于 2026-09-16 通过（标准与计划任务自启、方式切换、普通权限托盘）；发布前在 Windows 11 上重复登录检查。
- 推送、GitHub Release 与 Partner Center 提交等待明确批准。
- 用最终商店包验证退出后和运行中的真实通知点击、按钮及输入选择。
- ARM64 真机运行、商店 flight 升级和 WACK 仍需单独验证，交叉编译与包审计不能替代。
- 获准发布时填写实际发布日期，并验证 README 下载链接。

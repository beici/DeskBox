# DeskBox 1.5.2

DeskBox 1.5.2 is a focused hotfix for a 1.5.1 regression reported through the in-app feedback channel: file widgets could cap their contents at the first 30 items. Folders at or below the render threshold list every item again, and larger folders extend the rendered window whenever the visible area is not yet filled, so the incremental large-folder rendering introduced in 1.5.1 can no longer dead-lock at its initial slice.

Release date: 2026-09-15 (prepared from the 2026-09-15 working tree).

## Highlights

- **File widgets show everything again.** 1.5.1's large-folder render windowing shipped with its activation threshold declared but never enforced, so every folder started at a 30-item prefix, and the only growth trigger was the scroll event — a prefix that fit inside the viewport had nothing to scroll and stayed stuck at 30 items forever (most visible on tall widgets). The threshold is now enforced where the window is computed: folders up to 300 items render in full, exactly as before 1.5.1. Larger folders grow the rendered window after every layout pass while the visible area is not yet filled, in addition to the existing scroll-near-the-end and End-key growth paths, so the deadlock is gone for every folder size.

See `CHANGELOG.md` for the complete list.

## Downloads and verification

### GitHub Releases installers

| File | Architecture | Size | SHA-256 |
| --- | --- | ---: | --- |
| `DeskBox_Setup_1.5.2_x64.exe` | x64 | 41.9 MB | `46F563D3E12D7090CAF3D8164615EAEBED3E15EBCD28E4D3C7030CCA5F727180` |
| `DeskBox_Setup_1.5.2_arm64.exe` | ARM64 | 41.4 MB | `4CDB9895789784A5A23B5CC0B23682291BB702859EECB119DE5D0B6A2B178FD5` |

Both rows are from the 2026-09-15 build of the final working tree (3539/3539 tests); if the tree changes again before release, rebuild and recompute the whole table. The direct installers are Full Native AOT builds with the bundled private Windows App Runtime. Installers are currently unsigned; signing changes the hash, so recompute after signing.

## Validation

- x64 full test suite: 3539/3539 passed (2026-09-15, final working tree = 1.5.1 plus the render-window fix).
- The threshold enforcement and the fill-the-viewport growth are pinned by two new contract tests in `RenderWindowContractTests` (folders within the activation threshold render in full; growth must run when the rendered prefix does not overflow the viewport).
- x64 and ARM64 Native AOT retail publish and Full installer packaging verified locally on 2026-09-15.
- Store package: dual-architecture merged msixupload (`DeskBox_1.5.2.0_x64_arm64.msixupload`) built and audited from the same tree.
- Pending for release day: second-machine walkthrough of the validation checklist (60-file folder in a tall widget should list every item; a folder above the threshold should fill the viewport and keep loading on scroll), final hash table confirmation against the published assets.

## Remaining release-boundary checks

- Confirm the README download links resolve after the GitHub release is published.
- Backfill both installer rows (size and SHA-256) after rebuilding.
- Store package submission follows the direct-release checklist.
